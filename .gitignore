# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase CLI cache (downloaded function backups, etc.)
supabase/.temp/

# Local rollback snapshot
codef-sync-rollback.ts

# Notion 연동 임시/로컬 스크립트 (토큰 하드코딩 가능 — 절대 커밋 금지)
_notion-*.mjs
notion-token.txt

# temp/local junk (실수 커밋 방지)
.tmp-pdf/
.tmp_codef.txt
.tmp_*.json
*.pdf
_QA_LOG_*.md
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json
.tmp_qa/
.env.local

# 핸드오프/QA 산출물 — 사장님 지시(2026-07-06): 커밋/푸시에 안 올리기.
#   handoff/qa 폴더 전체 + 저장소 루트에 떨어지는 스크린샷/압축본은 로컬 전달용이라 git 이력 오염.
#   앱 자산(public/, src/ 하위)은 대상 아님 — 아래 패턴이 하위 디렉터리를 매칭 안 함.
handoff/
qa/
/*.zip
/*.png
/*.jpg
/*.jpeg
*.hwp
*.hwpx
*.docx
*.pptx
*.xlsx
.env*
