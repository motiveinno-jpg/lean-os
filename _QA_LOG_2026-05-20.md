# QA 처리 로그 — 2026-05-20 (notion-agent 참조용)

## 처리한 직원 QA 22건 (시간 순, origin/main 최신=a7b6c68b, edge send-signature-email v19)

### 1. [전자계약] 단체 일괄발송 3건 중 1건 발송실패 — `dfaa430f`
- **증상**: 단체 일괄 서명 발송 후 토스트 "발송 2건 / 실패 1건". 어느 거래처가 왜 실패했는지 표시 없음.
- **원인 진단**: DB batch `d22d95f4-36dc-462f-a940-ada5274b0355` 분석. 3건 모두 동일 이메일(`ekwjd5926@mo-tive.com`) 동시 발송. 이메일 제공자 일시 rate-limit 으로 1건 거부 추정. DB는 모두 status='sent' (서명 링크는 유효), 토스트 카운트만 1건 실패.
- **수정**:
  - `src/lib/signatures.ts`: `sendSignatureEmail` 1회 자동 재시도(1.2초 백오프). `createBulkSignatureRequestsToOrgs` chunk 5→3 + chunk 사이 250ms 간격.
  - `src/app/(app)/signatures/page.tsx`: `BulkOrgSendModal.submit` toast 에 실패/스킵 첫 1~2건 사유 노출. "• {거래처명}: {reason}\n외 N건".
- **화면 위치**: `/signatures` → "+ 단체 일괄 발송" 모달 → "발송"

### 2. [카드] 이용대금청구서 아래 체크/직불/기타 접기버튼 — `8c8e003`
- **수정**: `src/components/card-billing-summary.tsx`. 체크·직불·기타 영역(line 293)을 접기 토글, 기본 **접힘**. `localStorage('card_billing_noncredit_collapsed')`.
- **화면 위치**: `/cards` → "이용대금 청구서" 카드 하단

### 3. [카드] 월별 사용금액 카드 접기버튼 — `8c8e003`
- **수정**: `src/components/card-insights.tsx`. 카드별 표(line 436)를 접기 토글, 기본 **접힘**. 막대 그래프는 항상 표시. `localStorage('card_monthly_usage_percard_collapsed')`.
- **화면 위치**: `/cards` → "📊 카드 월별 사용금액" 패널 → "카드별 N장" 토글

### 4. [카드] space-y-2 구역(Registered Cards List) 제거 — `8c8e003`
- **수정**: `src/app/(app)/transactions/page.tsx`. 카드 탭의 `<div className="space-y-2">`(line 1858~1897) 제거. 이용대금 청구서·월별사용금액·CODEF 카드별 사용액에서 이미 모든 카드 정보 노출되어 중복.
- **화면 위치**: `/cards` (영역 자체가 사라짐)

### 5. [근태] 갭③ checkOut 후 recompute chain — `311fcdba` (rebase 후 hash)
- **증상**: 직원이 출퇴근시간 설정 후 지각 + 연장근무했는데 근태 상세에 "정상 출근"으로 찍히고 야근수당/지각 표시 안 됨.
- **원인**: `checkOut` 함수가 attendance-checkin Edge Function 호출만 하고 그 뒤 `recomputeAttendance` 트리거 안 함. `is_late/late_minutes/overtime_minutes/night_minutes/holiday_minutes` 컬럼이 빈 상태.
- **수정**: `src/lib/hr.ts` L757 `checkOut`. invokeAttendance 호출 후 동일 employeeId+해당 일자에 `recomputeAttendance({ from=date, to=date })` chain. `recomputeMonthlyAllowances` 도 내장 chain. 부분 실패는 console.warn(client only), 체크아웃은 성공 처리.
- **화면 위치**: 근태 페이지 출근/퇴근 버튼 (`/attendance` 또는 `/employees > 근태 탭`)

### 6. [근태] 갭① 표 뷰 배지 매핑 — `b4494292` (rebase 후)
- **증상**: attendance_records 의 6 컬럼이 화면에 전혀 표시 안 됨 (grep 0건). status='late' 만 보이고 "지각 N분"은 안 보임.
- **수정**: `src/app/(app)/employees/page.tsx` AttendanceTab Table View. 상태 셀(line 4147) 에 분 단위 상세 배지 묶음 추가:
  - 🔴 지각 {late_minutes}분 / 🟠 연장 {h}h {m}m / 🟣 야간 {h}h {m}m / 🟢 휴일 근무 / 🔵 외근·당직·원격·출장
  - `flex flex-wrap` 으로 모바일 가독성
  - 연장 컬럼은 `overtime_minutes` 우선, 없으면 `overtime_hours` fallback (회귀 0)
- **회귀 0**: 캘린더 뷰 미변경(점 형태 유지), 관리자 isAdmin 수정 버튼 그대로, 직원·관리자 동일 적용
- **화면 위치**: `/attendance` 또는 `/employees > 근태 탭` → Table View

### 22. [직원 위젯 → 프로젝트 화면 라우팅 + /deals employee 분기] — `5889ccb3` + `a7b6c68b`
- **피드백**: "위젯 누르면 모달만 뜨고 프로젝트 화면으로 안 넘어가." 직전 read-only 모달은 사용자 기대와 다름.
- **A `5889ccb3`** dashboard 위젯:
  - `useRouter` import 추가
  - 카드 onClick: `setOpenDeal(d)` → `router.push('/deals?id=' + d.id)`
  - `openDeal` state · 모달 JSX 정리 (§3 본인 만든 흔적)
  - 안내 문구: "재무 정보 비공개" 명시
- **B `a7b6c68b`** /deals DealDetailView employee 분기:
  - **isLimitedView 헬퍼** = `isPartnerView || isEmployeeLimited` (partner 패턴 일반화)
  - **B-1 편집·아카이브 가림** 3곳 (`!isPartnerView` → `!isLimitedView`): 수정 버튼/아카이브 버튼/DealAssigneesPanel canEdit
  - **B-2 재무 가림** 6곳:
    · L299 계약금 span 가림 (기간 span 유지)
    · L309-314 4 KPI grid (총매출/총비용/마진/작업단계) — 직원은 작업단계만 단독 카드로 노출
    · L315 DealPipelineWidget (매출/비용 흐름) 가림
    · L316 견적 품목 / 결제 비율 카드 통째 가림
    · L359 비용 내역 조건문에 `!isLimitedView &&` 추가
    · L360 외주(서브딜) 조건문에 `!isLimitedView &&` 추가
  - **B-3 본인 담당 검증 + redirect**:
    · `useQuery` `get_my_assigned_deals` RPC → `Set<string>` 정규화 (O(1) lookup)
    · `useEffect`: `dealId` 가 Set 에 없으면 toast 에러 + `router.replace('/dashboard')`
    · owner/admin/partner 는 `enabled=false` (RLS 가 회사격리 처리)
- **회귀 0**: partner 분기 동일(`isLimitedView` 안에 포함), owner/admin 무영향, get_my_assigned_deals RPC 변경 0, 비재무 영역(이름·기간·상태·역할·채팅·활동로그·할당패널·ProjectBoard) 유지, 채팅/문서 컴포넌트 무수정
- **화면 위치**: 직원 대시보드 "내 프로젝트" 위젯 카드 클릭 → `/deals?id=<id>` (재무 0·편집 0·작업단계만)

---

### 21. [UX/partners] 거래처 표 sticky 헤더 + 페이지네이션 (1e8bb2b 패턴 미러) — `b536d7f2`
- **요구**: 100개+ 등록 시 보기 힘듦 — 제목줄 고정 + 페이지 처리
- **수정** (`src/app/(app)/partners/page.tsx`):
  - `page`/`pageSize(20)` state + `useEffect` page=1 reset (debouncedSearch/typeFilter/activeFilter/classFilter/regionFilter/sizeFilter/pageSize 변경 시)
  - 메인 표 컨테이너 `overflow-x-auto` → `overflow-auto max-h-[60vh]` (sticky 동작 부모 scroll)
  - `<thead>` 에 `bg-[var(--bg-card)] sticky top-0 z-10`
  - `partners.map(...)` → `partners.slice((page-1)*pageSize, page*pageSize).map(...)`
  - 표 아래 페이지네이션: "전체 N건 중 a–b" + pageSize select (20/50/100) + ‹ {page} / {totalPages} ›
- **회귀 0**: partners.length=0 시 미노출, 360도뷰 패널 안 N개 표 미터치, 데이터/필터/RLS/마이그 무변경
- **패턴 일관**: 1e8bb2b (signatures/documents) U4/U5 UX 동일
- **화면 위치**: `/partners` (좌측 메인 표)

---

### 20. [양방향 서명] 갑(우리) 서명·도장 단계 추가 — pending_our_signature → fully_signed — `0d79670a` + `d21645b2`
- **사용자 지적**: "거래처가 서명·도장 찍어서 보내면 우리는 언제 싸인·도장?" 단방향만 구현돼 있어 진정한 전자계약 미완성.
- **새 흐름** (stage='contract' 만 적용 — 견적 등은 기존 단방향 그대로):
  1. 갑(우리) 계약서 발송 → status='sent'
  2. 을(거래처) 외부 서명·승인 → **status='pending_our_signature'** (deal.stage 미전환)
  3. 갑(우리) 패널 "📝 우리 서명·도장 추가" → 모달 → 손글씨/타이핑/도장 → submit_our_signature RPC
  4. **status='fully_signed'** + deal.stage 자동 전환 (contract→in_progress)
  5. 양측 서명 합성된 본문을 `/contracts/signed/<id>` 에서 양쪽 노출
- **A `0d79670a` DB**:
  - 컬럼 5개 (`our_signature_method`/`our_signature_data_url`/`our_signed_at`/`our_signer_user_id`/`fully_signed_contract_url`)
  - `status` CHECK 확장 (`pending_our_signature`/`fully_signed` 추가, 기존 6값 무손실)
  - `submit_our_signature` RPC 신설 — SECURITY DEFINER, `is_company_admin`+본인 회사+status='pending_our_signature' 권한, `signed_contract_html` 갱신 + status='fully_signed' + `deal.stage` 자동 전환, `audit_logs 'our_sign'` 기록
  - `submit_quote_decision` 본문 수정 (시그니처 12-arg 그대로): stage='contract' + approved → `pending_our_signature` (deal.stage 미전환), 그 외 stage 는 기존 'approved' 그대로
  - 알림 메시지 분기: `"거래처 서명 완료 — 우리 서명 대기 (contract)"`
- **B+C `d21645b2` UI**:
  - `quote-approvals.ts`: `QuoteApprovalStatus` 2값 추가, `ApprovalLite` our_* 3 필드, `getLatestApproval` SELECT 확장, `STATUS_LABEL/TONE` 매핑 ('우리 서명 대기' warn, '최종 성립' positive)
  - `contract-stage-card.tsx`:
    · `PendingOurSignatureCard` — pending 상태 카드 (거래처 방식·시각 표시 + "📝 우리 서명·도장 추가" 버튼)
    · `OurSignatureModal` — `SignatureCapture` 재사용 (draw/type/upload) + **회사 직인 자동 채움** (`companies.seal_url` 있으면 "직인 자동 적용" 버튼)
    · `SignedContractCard` 확장 — `fully_signed` 도 표시
    · `fetchSignedHtml` 헬퍼 — 양측 합성 시 기존 HTML fetch
    · 합성 HTML — 기존 (을 서명까지) 끝에 갑 서명 카드(emerald 강조) append
- **회귀 0**: estimate/progress/completion/settlement 무영향, 외부 페이지 코드 변경 0, `/contracts/signed/<id>` 페이지 코드 변경 0 (HTML 양측 합성 자동 렌더), 비재귀 RLS 헬퍼만
- **화면 위치**: 외부 거래처 서명 후 → 회사 알림 "거래처 서명 완료 — 우리 서명 대기" → `/projects` 패널 contract stage 카드 → "우리 서명·도장 추가" → 모달 → 최종 성립

---

### 19. [메일·문서 3종 보강] 결제단계 금액 + 갑/을 사업자등록번호 + 외부 페이지 을 입력 폼 — `43d8d3d5` → `04afd3db` (3 commit + edge v19)
- **증상**: 직원 첨부 이미지 — ① 결제단계 메일에 % 만 있고 금액 없음 ② 견적/계약서 갑(우리 회사) 채워지나 을(거래처) 회사명·대표자 빈칸 ③ 사업자등록번호 양쪽 미표시
- **A `43d8d3d5`** 결제단계 % + 금액:
  - `supabase/functions/send-signature-email/index.ts`: `InvokeBody.paymentStages` + `buildQuoteHtml` 결제단계 표 신설 (N차 / 단계명 / ratio%+₩금액 / 조건). 총액(p.amount) 있으면 `₩{ratio×amount/100}` 자동 환산
  - mcp `deploy_edge_function` v18→v19 prod 재배포 완료
  - `project-quote-stages.tsx` handleSend/Resend invoke body 에 amount/items/paymentStages 추가 (estimate 단계 메일도 결제단계 표시)
- **B `1df72aba`** 갑/을 사업자등록번호:
  - 마이그 `20260521050000` — RPC 9→12 arg 확장 (`p_signer_company_name/business_number/representative`), 본문 `payload` jsonb `jsonb_strip_nulls` 후 merge (approved 만)
  - 시스템 양식 3종 body_html UPDATE — 갑/을 영역에 "사업자등록번호: {갑_사업자번호}" 줄 추가, 신규 변수 형식 `{갑_회사명}/{갑_사업자번호}/{갑_대표자}/{을_*}` 6 토큰, variables JSONB 동기화 (11 토큰)
  - `src/lib/contract-templates.ts` `buildContractVarsFromDeal` 확장 — myBusinessNumber/partnerBusinessNumber + 신규 6 토큰 + alias(`갑사명`/`대표자_갑`/`을사명`/`대표자_을`) 호환
  - `ContractStageCard`: companies/partners select 에 `business_number` 추가, `{지급조건}` 텍스트에 금액 합성 (`"선금: 30% (₩30,000,000) — 계약 후 7일"`)
- **C `04afd3db`** 외부 페이지 을 입력 폼:
  - `/quote/[token]` 서명 모달에 input 3개 (회사명 필수, 사업자등록번호 선택, 대표자명 필수)
  - contract 승인 유효성: 회사명·대표자 누락이면 "회사명과 대표자명을 입력해 주세요"
  - submit RPC 12-arg 호출 — `p_signer_*` 3개
  - `signed_contract_html` 합성 시 `template_snapshot_html` 의 `{을_*}` 토큰 입력값으로 치환 (+ v1 alias `{을사명}/{대표자_을}` 호환)
  - 본문 끝 서명란 카드도 사업자번호·대표자 노출
- **효과**: 회사 패널 "📄 서명된 계약서 보기" → 계약서 갑/을 양쪽 완전 채워진 상태, 사업자등록번호 양쪽 노출, 거래처 서명/도장 + 감사 메타
- **회귀 0**: estimate stage 100% / 기존 회사 양식 alias 자동 매핑 / RPC payload jsonb merge 다른 컬럼 무영향
- **화면 위치**: 거래처 메일함(결제단계 표) / `/quote/<token>` 서명 모달(입력 폼) / `/contracts/signed/<id>` (갑·을 풀필)

---

### 18. [결제단계 UX] % 입력 시 금액 자동 표시 + 합계 영역 보강 — `3f7741dc`
- **요구**: 프로젝트 계약서 돈 탭의 결제단계에서 ratio % 만 보이고 금액 환산을 사용자가 머리로 계산. ratio 입력 시 자동으로 금액 표시 (예: "30% = ₩30,000,000").
- **수정 (`src/components/project-quote-stages.tsx`)**:
  - 행별 (L469): `% ` 뒤에 `= ₩{Math.round((contractTotal × ratio) / 100).toLocaleString()}` 추가
  - 합계 (L447): `(₩{합계금액} / 계약가 ₩{contractTotal})` 추가 — 분배 합계 vs 계약가 정합 한눈에
  - contractTotal=0 시 금액 hidden (빈 "₩0" 노이즈 회피)
- **동작**: ratio 입력 즉시 갱신, contractTotal 변경 시 모든 단계 자동 재계산, readonly 모드에서도 정상, estimate/contract 양쪽 자동 적용
- **회귀 0**: stages/stage.ratio/contractTotal 데이터 흐름 무변경, updateStage/addStage/removeStage 로직 무변경
- **화면 위치**: `/projects` 패널 → 돈 탭 → 견적/계약서 카드 → 결제 단계 섹션

---

### 17. [계약서 서명·도장 + 서명본 회수] 외부 서명 + 회사 내부 렌더·인쇄 — `25615512` → `dbe527a6` (4 commit)
- **요구**: ① 외부 `/quote/[token]` stage='contract' 승인 시 손글씨/타이핑/도장 입력 ② 서명본이 회사 슬라이드 패널에 회수
- **수정 (4 commit 분할)**:
  - **A `25615512`** DB: `quote_approvals` 7 컬럼 + `submit_quote_decision` RPC 3→9 arg 확장
    - signature_method/signature_data_url/signed_contract_url/signed_contract_html/signer_ip/signer_user_agent/signed_at_external
    - PostgREST overloading 충돌 방지 위해 3-arg DROP + NOTIFY pgrst reload
    - GRANT EXECUTE TO anon (외부 페이지가 RPC 호출)
    - RLS 무수정 (회사격리 정책 자동 적용)
  - **B `5bad7115`** 외부 서명 UI: `SignatureCapture` 신규 + `/quote/[token]` contract 분기
    - 3 모드: draw(canvas pointer, DPR) / type(Nanum Myeongjo italic bold) / upload(FileReader)
    - contract stage 승인 → 서명 모달 → signed_contract_html 합성 (본문 + 서명란 div, indigo 강조)
    - 거절/estimate 100% 그대로 (회귀 0)
  - **C-1 `02c0d99d`** 패널 회수 표시: ApprovalLite 확장 + `SignedContractCard`
    - signature_method/signed_at_external/signer_ip/has_signed_html/signed_contract_url 5 필드
    - `getLatestApproval` SELECT 확장, has_signed_html 정규화 (HTML 본문은 별도 페이지 fetch)
    - approved 상태에서 "📄 서명된 계약서 보기" + 방식 라벨(✍️/🖊/🟥) + 감사 메타
  - **C-2 `dbe527a6`** 렌더 페이지: `/contracts/signed/[id]` 신규
    - supabase select quote_approvals + deals + companies JOIN (RLS 회사격리)
    - signed_contract_html dangerouslySetInnerHTML + window.print() (Ctrl+P → PDF 저장)
    - @media print 글로벌 — 헤더·감사 카드 hidden, A4 18mm margin
- **단순화 결정 (자율)**: PDF Storage 업로드 대신 `signed_contract_html` DB 컬럼 + 회사 내부 페이지 print-to-PDF
  - 이유: jsPDF/html2canvas 의존성 0, Storage bucket 정책 0, RLS 만으로 보호
  - PDF Storage 는 별건 후속 (signed_contract_url 컬럼만 미리 만들어둠)
- **인프라 재사용 (별도 신설 0)**:
  - sign 페이지 canvas DPR 패턴 차용
  - Nanum Myeongjo 폰트 (직인 카탈로그에서 이미 import)
  - quote_approvals RPC/RLS 그대로 + 컬럼만 ADD
- **회귀 0**: 견적 stage 100% / 단체일괄(9803950) / 1단계 P0 / 근태 신기능 무영향
- **화면 위치**:
  - 외부 서명: `/quote/<token>` (stage='contract' 만) → "✍️ 서명 후 승인" 버튼 → 모달
  - 회사 회수: `/projects` 패널 → 돈 탭 → 계약 승인 후 "✅ 계약 승인 완료" 카드 → "📄 서명된 계약서 보기"
  - 인쇄: `/contracts/signed/<id>` → "🖨 인쇄/PDF 저장"
- **별건 후속 (이번 라운드 X)**: PDF Storage 자동 생성, 서명 위치 드래그앤드롭, 다중 서명자, 한국 전자서명법 부합성 검토

---

### 16. [계약서 양식 카탈로그] 시스템 3종 + 회사 자체 양식 + contract stage 분기 — `a1db8749` → `9dfc373e` (5 commit)
- **증상**: 견적→계약 stage 전환 후 "계약서 발송" 누르면 견적서 본문(품목/결제단계) 그대로 표시. 계약서 양식 개념 없음.
- **요구**: ① 기본 제공 양식 ② 회사 자체 양식 업로드 ③ 양식 추가 관리 ④ 계약 단계에서 양식 선택해 발송
- **수정 (5 commit 분할)**:
  - **A `a1db8749`** DB: `contract_templates` 신규 테이블 + 비재귀 RLS + supabase_realtime publication
    - 핸드오프 명세 `deal_contract_templates` prod 부재 + `doc_templates` 는 hr 영역이라 신설
    - is_system XOR company_id CHECK, body/file 최소 1개 CHECK
    - SELECT: is_system OR get_my_company_id(), WRITE: admin only + 시스템 차단 (4 policy)
  - **B `76b4d54d`** seed: 시스템 양식 3종 (service/supply/consulting)
    - 한국 표준 8조 + 9 변수 ({갑사명}/{을사명}/{대표자_갑}/{대표자_을}/{계약금액}/{계약기간_시작}/{계약기간_종료}/{지급조건}/{특약})
    - prod 3행 정상 삽입
  - **C `f87c0a4d`** lib + UI + settings 마운트
    - `src/lib/contract-templates.ts`: ContractTemplate 타입 + CRUD + extractVariables + renderTemplateWithVariables + buildContractVarsFromDeal
    - `src/components/contract-templates-manager.tsx`: 시스템 read-only + 회사 자체 CRUD + 모달(HTML/Markdown/PDF 업로드) + 변수 자동 감지 칩
    - `/settings?tab=company` 회사 정보 탭에 마운트 (CompanyInfoTab 아래)
  - **D `4f27754f`** project-quote-stages contract 분기 + `ContractStageCard` 신규 컴포넌트
    - 양식 선택 드롭다운 ("🔒[시스템]" / "✏️[자체]" + "양식 관리 →" 링크)
    - 변수 자동 채움 (회사/거래처/계약금액/지급조건) + 누락 변수 입력 (계약기간_시작/종료/특약)
    - 미리보기 (renderTemplateWithVariables 결과 HTML) 또는 PDF 링크
    - 발송 payload 확장: `{ template_id, template_snapshot_html, variables, contract_period, special_terms }`
    - 엣지 invoke `stage='contract'` 전달 (직전 라벨 동적화 `8a3696fb` 와 매칭)
  - **E `9dfc373e`** `/quote/[token]` 외부 페이지 contract 본문 렌더
    - payload.template_snapshot_html `dangerouslySetInnerHTML` (max-h 500 스크롤) 또는 PDF 링크
    - 기존 견적 품목/결제 단계는 estimate 만 렌더 (회귀 0)
- **인프라 재사용 (별도 신설 0)**:
  - quote_approvals RPC (createApproval/sendApproval/resendApproval/submit_quote_decision) 그대로
  - send-signature-email 엣지 v18 (stage 라벨 동적화) 그대로
  - 알림 라우팅 (notification-center quote_approval 분기, `9a263ed2`) 그대로
- **회귀 0**: estimate 100% 그대로 / quote_approvals RPC/RLS/엣지 무수정 / 단체일괄(9803950) 무영향 / 1단계 P0 영향 0
- **화면 위치**:
  - 양식 관리: `/settings?tab=company` → "계약서 양식 관리" 카드
  - 계약서 발송: `/projects` → 프로젝트 패널 → 돈 탭 → (stage='contract' 일 때 자동) → 양식 선택 + 변수 입력 + 발송
  - 외부 검토: 거래처가 받은 메일 CTA → `/quote/<token>` → 계약서 본문(snapshot HTML) 표시

---

### 15. [알림 라우팅] 견적/계약 거절·승인 알림 → 해당 프로젝트 견적/계약 화면 직접 점프 — `9a263ed2`
- **증상**: 외부 거래처가 견적/계약을 승인 또는 거절하면 owner/admin 에게 알림이 오는데, 클릭하면 `/projects` 일반 페이지로만 가서 어떤 프로젝트의 견적인지 계약인지 찾아 들어가야 함.
- **원인**:
  - `submit_quote_decision` RPC 가 notifications 에 `entity_type='quote_approval'` + `entity_id=quote_approvals.id` 로 INSERT
  - `notification-center.tsx` `getEntityRoute` 에 quote_approval 매핑 없음 → prefix fallback `/approvals`
  - `/notifications` 페이지 `ENTITY_HREF.quote_approval = () => '/projects'` — deal_id 정보 없는 일반 점프
- **수정**:
  - `src/components/notification-center.tsx`:
    · `getEntityRoute` case 'quote_approval' 추가 (fallback `/projects`)
    · `handleNotificationClick` 에 비동기 분기 — `supabase.from('quote_approvals').select('deal_id, stage').eq('id', entity_id)` → `router.push('/projects?deal=<id>&action=<contract|quote>')`
  - `src/app/(app)/notifications/page.tsx`:
    · rows fetch 시 `quote_approval` entity_ids 추려 한 번에 `quote_approvals` prefetch → `quoteMap[entityId] = { deal_id, stage }`
    · Link href 결정 시 quoteMap 있으면 `/projects?deal=<id>&action=<action>` 점프
    · `stageToAction(stage)` 헬퍼 (contract → 'contract', 그 외 → 'quote')
  - `project-slide-over.tsx` 의 `ACTION_TAB` 매핑이 이미 'quote'/'contract' → `{ tab:'money', scroll:'sec-quote' }` 처리 → 패널 열리면 자동 견적/계약 카드 스크롤 + 1.5초 ring 강조 (PR3.5 패턴 재사용)
- **회귀 0**: DB / RPC / RLS / 마이그 / 엣지 무변경. 다른 entity_type 라우팅 무영향. `quote_approvals` RLS `select_admin_or_self` 통과 — owner/admin 본인 회사 행만 fetch.
- **효과 (배포 후)**: 알림 "거래처 거절 — contract" 클릭 → 해당 프로젝트 슬라이드 패널 자동 오픈 + 돈 탭 + sec-quote 자동 스크롤
- **화면 위치**: 알림 벨(헤더) / `/notifications` 페이지 → quote_approval 알림 클릭

---

### 14. [견적 워크플로우] 메일·외부페이지·엣지 stage 라벨 동적화 — 계약서가 견적서로 표시 — `8a3696fb` + edge v18
- **증상**: 계약서를 발송했는데 메일 제목·본문·CTA 모두 "견적서"로 표시. 외부 페이지 방문해도 `<h1>견적서 확인 요청</h1>` 헤더. 본인이 보낸 게 계약서인 줄 모름.
- **원인 진단** (DB 데이터로 클라/서버 분리 검증):
  - 최근 stage='contract' 행 2건(`81816a57` viewed, `07476ea0` rejected) 모두 DB 정상 저장
  - 즉 클라 `ProjectQuoteStages` 는 정확히 stage='contract' 로 발송 중
  - 누락된 3곳:
    1. `send-signature-email` 엣지 함수 — "견적서 확인 요청" / "견적서 확인 및 결정하기" / 메일 제목 `[견적서]` 모두 hardcoded
    2. `/quote/[token]` 외부 페이지 — `<h1>견적서 확인 요청</h1>` / "견적 품목 (N건)" hardcoded
    3. 클라 `functions.invoke` body 에 `stage` 파라미터 미전달
- **수정** (3곳 동시):
  - `supabase/functions/send-signature-email/index.ts` (재배포 v17→v18):
    · `InvokeBody.stage?: QuoteStage` 추가
    · `STAGE_LABEL_KO` / `STAGE_HEADER_HINT` / `STAGE_CTA` 3매핑 신설 (estimate:견적서, contract:계약서, progress_report:진척 보고서, completion:완료 확인서, settlement:정산 확인)
    · `buildQuoteHtml(p.stage)` — 헤더 / 본문 안내 / CTA 버튼 / 메일 제목 `[${stageLabel}]` 모두 stage 별
    · stage 안 오면 'estimate' default (회귀 0)
    · `mcp__claude_ai_Supabase__deploy_edge_function` 으로 prod 재배포 완료 (verify_jwt=true 기존 유지)
  - `src/app/quote/[token]/page.tsx`:
    · `STAGE_LABEL_KO` map + `stageKo(row.stage)` 헬퍼
    · 헤더 `<h1>견적서 확인 요청</h1>` → `<h1>{stageLabel} 확인 요청</h1>`
    · 품목 표 라벨 동적
    · `row.stage` 는 RPC `get_quote_approval_by_token` 응답에 이미 포함 (스키마 무변경)
  - `src/components/project-quote-stages.tsx`:
    · `handleSend` / `handleResend` invoke body 에 `stage` 추가 → 엣지가 라벨 분기
- **회귀 0**: DB 마이그 / RPC / RLS 무변경. `type='signature'` 흐름 무영향. 기존 estimate 단계 100% 동일 (default stage).
- **라이브 검증** (직원):
  1. contract 단계 deal 에서 발송 → 메일 제목 `[계약서] ...`, 본문 헤더 "계약서 확인 요청"
  2. CTA "계약서 확인 및 결정하기" 클릭 → 외부 페이지 헤더도 "계약서 확인 요청"
  3. 동일 흐름: 진척 보고서 / 완료 확인서 / 정산도 stage 따라 자동 라벨
- **화면 위치**: 견적 패널 → "발송" 버튼 / 거래처 메일함 / `/quote/<token>` 외부 페이지

---

### 13. [견적 워크플로우] ProjectQuoteStages stage 다이내믹 분기 — `04bf49e1` + `20014ce3`
- **증상**: 견적 승인 후 `deal.stage='contract'` 자동 전환됐는데 슬라이드 패널이 견적 컴포넌트 그대로 + "이미 승인됨" 표시. "계약서 발송" 버튼이 어디에도 없음.
- **원인**:
  - `ProjectQuoteStages` (src/components/project-quote-stages.tsx) 가 `stage='estimate'` 만 hardcoded (L100/111/181/217/257 등)
  - lib(`quote-approvals.ts`)·DB RPC 는 stage 일반화 적용돼 있는데 컴포넌트만 안 따라감
  - `project-slide-over.tsx` L524 도 stage prop 안 전달
- **수정** (2 commit 분할):
  - `04bf49e1` ProjectQuoteStages 일반화:
    · Props 에 `stage?: QuoteApprovalStage` 추가 (default 'estimate')
    · `getLatestApproval` / `subscribeApprovalStatus` / `createApproval` 모두 `props.stage` 사용
    · useEffect 의존성 `[dealId, stage]` — 단계 변경 시 approval 재조회 + Realtime 구독 갱신
    · `STAGE_LABEL` = { estimate:견적서, contract:계약서, progress_report:진척 보고서, completion:완료 확인서, settlement:정산 확인 }
    · `STAGE_NEXT_HINT` — SendBar 만료 안내 stage 별
    · 헤더 / SendBar 버튼 / 이메일 제목 / 토스트 모두 stage 라벨 사용
    · `StageStubCard` — progress_report/completion/settlement 우선 stub (라벨 + 다음 단계 안내 + StatusBadge)
  - `20014ce3` slide-over MoneyTab 와이어링:
    · `dealStageToApprovalStage(deal.stage)` 매핑 (in_progress→progress_report / completed→completion / 나머지 동일)
    · `<ProjectQuoteStages key={approvalStage} stage={approvalStage} ...>` — `key` 로 단계 전환 시 재마운트
- **E2E 흐름** (예상):
  1. 견적 작성·발송 → 외부 승인 → `submit_quote_decision` RPC 가 `deal.stage='contract'` 자동 전환 (마이그 20260520180000 적용됨)
  2. 패널 Realtime 으로 `deal.stage='contract'` 인식 → React-Query invalidate → MoneyTab 의 `approvalStage='contract'` 갱신
  3. ProjectQuoteStages 가 key 변경으로 재마운트 → stage='contract' 로 동작:
     · 헤더 "계약서 항목 / 결제 단계"
     · SendBar "📤 거래처에 계약서 발송" + "거래처가 승인하면 자동으로 진행 중 단계로"
  4. 계약서 승인 → `deal.stage='in_progress'` → progress_report stub 카드로 자동 전환
- **회귀 0**: estimate 기존 동작 100% 유지 (default prop). 데이터/RLS/엣지/마이그/lib 무변경. /deals 견적 편집 UI 별도 컴포넌트(PR3.5 잔여) 무영향. 1단계 P0 / 504 후속 / 근태 신기능 / 견적 PR-A~F 무영향.
- **화면 위치**: `/projects` → 프로젝트 슬라이드 패널 → "돈" 탭 → 최상단 견적/계약 카드 (stage 따라 자동 전환)

---

### 12. [🚨인시던트] 견적 발송 404 — generate_approval_token search_path 누락 — `1355c5d4`
- **증상**: 견적 발송 클릭 시 `POST /rest/v1/rpc/generate_approval_token 404 (Not Found)` → "발송 실패" 토스트.
- **사용자 핸드오프 추정**: "직전 504 응급조치(quote_approvals_touch / notify_quote_approvers DROP CASCADE) 가 generate_approval_token 등 다른 RPC 도 함께 DROP 했을 것."
- **실제 진단 결과** (다른 원인):
  - RPC 5종 **모두 prod 에 정상 존재** (generate_approval_token / get_quote_approval_by_token / mark_quote_approval_viewed / submit_quote_decision / resend_quote_approval)
  - SECURITY DEFINER, GRANT EXECUTE TO authenticated, 시그니처 매칭 정상
  - RLS 정책 5종 정상, 트리거 없음(의도된 응급 DROP 상태 그대로)
- **진짜 원인**:
  - `generate_approval_token()` 본문이 `gen_random_bytes(32)` 호출 (pgcrypto)
  - pgcrypto 는 `extensions` 스키마에 설치돼 있으나 함수의 `SET search_path = public` 만 지정 → extensions 미참조 → `gen_random_bytes` 검색 실패
  - Postgres 에러 `42883: function gen_random_bytes(integer) does not exist`
  - PostgREST 가 호출 실패를 404 로 노출 → 클라는 토큰 못 받아 `createApproval` 실패
- **수정** (`supabase/migrations/20260521020000_quote_approvals_generate_token_search_path.sql`):
  - `CREATE OR REPLACE FUNCTION public.generate_approval_token()` 멱등 재정의
  - `SET search_path = public, extensions` (extensions 추가)
  - 본문에서 `extensions.gen_random_bytes(...)` 풀패스 명시 (search_path 의존 제거)
  - GRANT 보존 (REVOKE PUBLIC/anon, GRANT authenticated/service_role)
  - `NOTIFY pgrst, 'reload schema'` — PostgREST 캐시 즉시 갱신
- **prod 검증**: `SELECT generate_approval_token()` → 43자 토큰 정상 발급, 문자 패턴 `^[A-Za-z0-9_-]+$` 매칭. 다른 RPC 4종 본문 extensions 의존 0건 (재발 위험 없음).
- **회귀 0**: 함수 시그니처 동일, RLS/트리거/정책/테이블/다른 RPC 무변경. resend_quote_approval 도 generate_approval_token 호출 → 자동 픽스.
- **별건 보류** (STEP 4): notify_quote_approvers 비동기 큐 재설계 / publication 등록(직전 인시던트에서 처리 완료 `b02efdb5`) / send-signature-email 엣지 재배포(e9b3f30d).
- **화면 위치**: 견적 패널 → "발송" 버튼

---

### 11. [🚨인시던트] 견적 Realtime publication 누락 — WebSocket fail · auth 504 hang — `b02efdb5`
- **증상**: 견적 발송 클릭 시 콘솔 WebSocket(wss realtime) 연결 실패 반복 + `auth/v1/user` 504, 발송 2분 hang. 사이트 자체는 정상.
- **원인**:
  - `subscribeApprovalStatus` (`quote-approvals.ts` L191) 가 `postgres_changes` on `quote_approvals` 구독
  - **20260520180000 마이그가 `supabase_realtime` publication ADD 빠뜨림** → 채널 거부 → 클라 retry 폭증
  - 같은 supabase 클라이언트의 다른 호출(`auth.getUser` 등) **리퀘스트큐 점거** → 발송 mutation hang
  - 점검 결과 `chat_events` / `chat_mentions` / `chat_reactions` 도 같은 누락 (realtime.ts 가 구독 중)
- **수정 (DB 마이그 2개, prod 적용 완료)**:
  - `supabase/migrations/20260521010000_quote_approvals_realtime_publication.sql` — quote_approvals ADD + REPLICA IDENTITY FULL
  - `supabase/migrations/20260521010100_chat_realtime_publication_complete.sql` — chat_events/mentions/reactions ADD
  - 검증: `pg_publication_tables` 에서 10개 구독 대상 테이블 모두 `supabase_realtime` YES (bank_accounts/transactions/cards/chat_*/quote_approvals/hometax/error_logs)
- **수정 (클라 안전망)**:
  - `src/lib/quote-approvals.ts subscribeApprovalStatus`: `.subscribe((status, err) => ...)` 콜백 추가
  - CHANNEL_ERROR / TIMED_OUT 2회 연속 → `removeChannel` + `reportError` 1회 + `disabled=true`
  - 영구 실패(publication·RLS) 시 리퀘스트큐 점거 차단 — auth.getUser 등 다른 호출 보호
- **메모리 신설**: `feedback_realtime_publication_gate.md` — 신규 테이블 `.channel()` 구독 추가 시 publication ADD + REPLICA IDENTITY FULL 체크리스트
- **회귀 0**: RLS/트리거/함수/라우트/엣지 무변경, 1단계 P0 / 견적 워크플로우 PR-A~F 무영향
- **화면 위치**: 견적 패널 → "발송" 버튼 (이제 정상, retry 폭증 없음)

---

### 10. [직인] 법인인감 비례 재조정 — 회사명 외곽 띠 꽉 차게 — `18378d8a`
- **증상**: 직원이 "(주)권테스트" 직인 자동생성했더니 회사명이 너무 작음. 안쪽 작은 테두리에 회사명이 작게 표시된 것처럼 느껴짐. 사용자 예시 이미지처럼 큰 외곽 띠에 꽉 차고 가운데 작은 원은 한자만 감싸야 함.
- **원인**: 직전(`4c720ec5`)에 폰트 깨짐(CJK fallback)은 해결했으나, **비례가 좁았음**.
  - 외곽 띠 두께가 size 의 8% 만 (outerR=0.475 / innerR=0.395) — 글자 폰트 상한이 size*0.115 (400px 에서 46px)
  - 짧은 이름(6자 미만)은 위쪽 호에만 모여서 더 빈약해 보임
- **수정** (`src/lib/seal-generator.ts drawCorporateSeal`):
  - outerR 0.475→0.480 / **innerR 0.395→0.310 (띠 두께 두 배 이상)** / centerR 0.168→0.210
  - **fontSize 상한 size×0.115 → size×0.18** (400px 캔버스에서 72px). ringThickness×0.85 가 메인 제약 — 띠 두께가 넓어졌으므로 글자가 띠 안에 큼직하게
  - **짧은 이름 균등 배치**: `denom = max(chars.length, 6)`. `chars.length < 6` 이면 12시 중심으로 좌우 펼침 → "(주)권테스트" 6자가 위쪽 중심으로 큼직하게 균등
  - 6 이상이면 시계방향 한 바퀴 균등 (12시 시작)
  - 외곽 굵은 원 0.026→0.028, 가는 원 0.010→0.011
  - 중앙 한자 fs = centerR×0.62 (centerR 자체가 25% 커져 절대 크기 증가)
- **회귀 0**: ensureSealFontsLoaded / DPR 2x 고해상도 / API 시그니처 / variant double/single/square 모두 무영향
- **확인 방법**: `/settings` → 회사정보 → 직인 자동생성 → 강력 새로고침 후 "자동생성" 다시 클릭

---

### 9. [UX/사이드바] 시스템 그룹 폐지·재배치 — `51eb3e5d`
- **증상**: 시스템 그룹에 14개 잡탕(채팅/승인/공지/결제/대출/매칭/금고/엑셀/내계정/요금제/가이드/에러로그/유저관리/회사설정). "지저분하다, 합치고 옮기고 없애줘."
- **수정**: `src/components/sidebar.tsx` NAV_GROUPS — owner/admin 사이드바를 4그룹 → 6그룹으로 재구성. 라우트/role/icon/badge/operatorOnly 모두 **보존**, 그룹만 재배치.
- **새 구조**:
  - 홈 (그대로) — dashboard / schedule / partners / projects / deals / board / notifications
  - **소통·결재 (신설)** — chat / approvals / announcements (시스템에서 3개 이동)
  - 인사관리 (그대로) — employees / team / signatures / my-contracts / attendance / documents
  - 회계관리 (그대로) — transactions / cards / tax-invoices / cash-receipts / reports / partners-ledger
  - **자금·결제 (신설)** — payments / loans / matching / vault / import-hub (시스템에서 5개 이동)
  - **설정·도움말 (신설)** — settings / mypage / billing / guide / error-logs(operatorOnly) / operator-users(operatorOnly) (시스템에서 4 + operatorOnly 2 이동)
- **순서 흐름**: 일상 작업(홈·소통·HR·회계) → 운영 도구(자금) → 설정/운영자. 사용 빈도 기준.
- **역할별 노출 카운트 (filterNavForRole 결과)**:
  - owner: 29개 (전 6그룹)
  - admin: 24개 (자금·결제에서 loans/matching/vault 자동 숨김 → 그 그룹에 2개)
  - partner: 홈 + 인사관리(documents) + 설정·도움말(mypage/guide) 일부 — 빈 그룹 자동 숨김
  - employee: `EMPLOYEE_NAV_GROUPS` 그대로(3그룹)
  - operator: 설정·도움말 그룹에 +2 (error-logs, operator-users)
- **미터치**: `EMPLOYEE_NAV_GROUPS`, `OWNER_TABS`(app-shell.tsx 모바일 5탭), `filterNavForRole` 로직, 라우트 추가/삭제 0, 데이터/RLS/엣지/RPC 무변경
- **회귀 0**: 1단계 P0 / Sentry / L 수당 / PR3~5 deals / 근태 / 직인 영역 모두 무영향
- **화면 위치**: 모든 페이지 좌측 사이드바 (모바일은 햄버거 드로어)

---

### 8. [직인] 자동생성 글자 깨짐 해소 — `4c720ec5`
- **증상**: 직인 자동생성 후 글자가 깨져 나옴. 외곽 회사명 한글이 □ 또는 잘못된 모양, 중앙 한자(代表理事)도 시스템 fallback 폰트로 두께·모양 불일치. 사용자 예시 이미지(빨강 이중 원형 + 외곽 한글 시계방향 + 중앙 代表理事 2x2)처럼 깔끔하게 안 나옴.
- **원인 3종**:
  1. **CJK 웹폰트 미로드**: `globals.css` 가 Inter + Pretendard 만 import. 한국어 명조(Noto Serif KR / Nanum Myeongjo)와 한자 폰트는 사용자 시스템 기본 fallback. Windows/Mac/Linux 마다 결과 달라짐.
  2. **그리기 직전 폰트 로드 미보장**: canvas 가 `document.fonts` 와 무관하게 즉시 그려서, Google Fonts 가 아직 다운로드 중이면 fallback 폰트로 그려짐.
  3. **저해상도 렌더**: devicePixelRatio 미적용. zoom 시 픽셀 비뚤어짐.
- **수정**:
  - `src/app/globals.css`: Google Fonts import — Noto Serif KR (500/700/900) + Noto Sans KR (700/900) + Nanum Myeongjo (700/800)
  - `src/lib/seal-generator.ts`:
    · FONT_STACK 강화 (Noto Serif KR → Nanum Myeongjo → Noto Sans KR → Pretendard → 시스템 → serif)
    · `ensureSealFontsLoaded()` 신설 — `document.fonts.load(...)` 로 한글·한자 폰트 미리 로드 + 1.5s 타임아웃 + `document.fonts.ready` 500ms 대기
    · 고해상도 렌더 (DPR 2x) — canvas.width = size × dpr, ctx.scale(dpr, dpr)
    · 외곽 회사명 호 배치 미세조정 — fontSize = min(ringThickness×0.95, arcCharWidth×1.5, size×0.115)
    · 색 보정 — #C0392B → #C8102E (선명한 인감 빨강)
    · 외곽 원 두께 0.022 → 0.026 (가독 향상)
- **회귀 0**: API 시그니처 동일 (generateCompanySeal/generateCompanySealDataUrl/SealOptions), variant double/single/square 무영향, 호출처(settings/page.tsx L1540/1559) 무수정
- **화면 위치**: `/settings` → 회사정보 → 직인 자동생성 → 회사명 입력 → "자동생성" 버튼

---

### 7. [근태] 갭② 설정 저장 후 30일 자동 재계산 — `f08ce18b`
- **증상**: 관리자가 출퇴근시간/야간/휴일/포괄임금 토글 등을 바꿔도 과거 attendance_records 에 반영 안 됨 (분 컬럼은 변경 전 정책으로 계산됨).
- **수정**: `src/components/hr-attendance-settings.tsx` saveMut.onSuccess. `recomputeAttendance({ companyId, from: today-30d, to: today })` 자동 호출. `recomputeMonthlyAllowances` chain 내장. 권한 부족/일시 실패 시 toast 에 사유 첨부, 저장 자체는 성공.
- **컴포넌트 자체는 이미 존재** (12 컬럼 + workdays_mask + 휴일 캘린더 + 한국 법정공휴일 seed). PR L 에서 구현됨. 이번 라운드는 자동 백필만 추가.
- **화면 위치**: `/settings?tab=hr_attendance` → "근무시간·가산수당" 카드 → "저장" 버튼

---

## 자율 모드 정책
- 메모리 `feedback_no_prompts.md`: yes 프롬프트 한 번도 안 뜨게 자동 진행.
- 코드 수정 / commit / push / Vercel 배포 / DB 마이그 / worktree / 파일 삭제 / 브랜치 전환 모두 묻지 말고 진행.
- 사용자가 직접 "물어봐" 요청한 경우만 확인.

---

## 배포
- 최종 origin/main: `f08ce18b` (rebase 후)
- 다른 창의 `quote-approvals` PR-A~F (`ea5a89ec`~`9a6b3335`) 6 commit 위에 우리 3 commit 적용
- Vercel 자동 배포 트리거됨 — 1~3분 후 www.owner-view.com 반영
- DB/RLS/RPC/Edge Function 변경 없음 (이번 라운드는 UI/lib 연결만)

## tsc/회귀
- `npx tsc --noEmit` exit 0
- payroll_items / attendance_records RLS / 비재귀 게이트 회귀 0
- attendance-checkin Edge Function 미접촉 (인터페이스 동일)
- L 수당 카탈로그 영역 무영향
- 단체일괄(9803950) 회귀 0

## E2E 시나리오 (배포 확인 후 직원 검증용)
1. **회사 출퇴근 설정**: `/settings?tab=hr_attendance` → 출근 09:00 / 퇴근 18:00 / 점심 60분 / 지각유예 0분 / 야간 22:00~06:00 → 저장
   → 토스트 "근태 설정 저장 + 최근 30일 자동 재계산 완료"
2. **직원 09:15 출근 → 21:30 퇴근**:
   - 근태 표에 "🔴 지각 15분" + "🟠 연장 2h 30m"
   - 22:00 이후 30분 → "🟣 야간 30m"
3. **회사 설정 변경 후 백필**: 출근시간 09:00→09:30 변경 → 저장 → 최근 30일 09:15 출근 행이 지각 → 정상으로 자동 재분류
4. **관리자 전체뷰**: 동일 배지 표시. 모바일 가독성(줄바꿈)
