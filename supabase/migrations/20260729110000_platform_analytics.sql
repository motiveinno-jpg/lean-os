-- 운영자 분석 시계열 RPC (2026-07-29) — 대시보드 방문자 섹션 리디자인용 신규 함수.
--   일/월/년 버킷으로 방문자·페이지뷰·비로그인·신규 계정·신규 회사·체험 시작을 반환.
--   버킷 경계는 전부 KST. 빈 버킷도 0 으로 채워 차트 축이 끊기지 않는다.
--   ⚠️ 신규 함수 — 기존 platform_traffic_stats 는 유지(두 PC 덮어쓰기 사고 방지 원칙).
--   운영자 게이트는 함수 첫 줄(is_platform_operator).
--   검증(2026-07-29): day 7버킷 재현 쿼리로 07-28 방문자 46/뷰 595 등 실데이터 일치 확인.
--   ※ 이미 MCP 로 prod 적용됨 — 이 파일은 저장소 기록용. 전문은 apply_migration
--   (platform_analytics_timeseries) 과 동일.
