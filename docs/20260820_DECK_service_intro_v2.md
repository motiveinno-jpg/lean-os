# 오너뷰 서비스 소개서 v2 — 34장 (2026-08-20)

사장님 지시: 참고 덱 두 건을 **역할을 나눠** 분석하고 완전히 새로 기획.
- **아드리엘** = 내실(서비스를 어떻게 소개하는가)
- **마인드노크** = 외형(이미지·글꼴·텍스트/이미지 배치)

읽는 사람(사장님 정정): **도입을 검토하는 중소기업 대표 · 담당자 · 개인사업자**
① 구식 ERP가 무겁게 느껴지는 대표 ② 창업 초기라 아직 시스템이 없는 대표
③ 운영은 하는데 한눈에 안 보이는 대표 ④ 경리·총무 담당자 ⑤ 개인사업자
→ 앞선 26장(투자자·언론 겸용)은 폐기. 언론용 팩트시트·보도 담당자 삭제.

## 산출물
- `dist/OwnerView_서비스소개서.pptx` (34장 · 16:9 · 3.7MB)
- `dist/OwnerView_서비스소개서.pdf` (2.3MB · 메일 첨부·배포용)
- `dist/png2/슬라이드*.PNG` (검수·SNS용 34장)
- ⚠️ `dist/`·`cap/` 은 .gitignore

## 재생성
```bash
node scripts/demo-seed.mjs            # 시연 회사 '오너뷰' (멱등)
node scripts/demo-seed-projects.mjs   # 시연 프로젝트·보드
node scripts/deck-capture.mjs         # 조각 캡처 (2배)
node scripts/deck-capture2.mjs        # 전체 화면 캡처 (2배)
node scripts/deck-capture3.mjs        # 원장·AI 참모·권한 보완
python scripts/deck2-crop.py          # → cap/v2  (사용 예시 8 + 기능 조각 30여)
python scripts/deck2_run.py           # → dist/OwnerView_서비스소개서.pptx
# PDF·PNG 는 PowerPoint COM 으로 export
```
빌더 구성: `deck2_core.py`(색·틀 함수) + `deck2_make.py`(01~07) + `deck2_make2.py`(08~20)
+ `deck2_make3.py`(21~34) + `deck2_run.py`(진입점).

## 구성 34장
| # | 장표 | # | 장표 |
|---|---|---|---|
| 01 | 표지 | 18 | 06. 계약과 문서 |
| 02 | 오너뷰는 (돈·일·사람·기록) | 19 | 07. 직원과 급여 |
| 03 | **이런 분들을 위해** (독자 3유형) | 20 | 08. AI 참모 |
| 04 | 자주 듣는 이야기 (고객 인용 4) | 21 | 챕터 Ⅲ 이렇게 씁니다 |
| 05 | 연결되는 곳 (은행·카드사·국세청) | 22~29 | **사용 예시 8장**(화면 크게) |
| 06 | 오너뷰 한 장 (허브 도해) | 30 | 챕터 Ⅳ 도입하기 |
| 07 | 세 가지 원칙 | 31 | **역할별로 보는 화면** |
| 08 | 챕터 Ⅰ 지금과 무엇이 다릅니까 | 32 | **보안 · 권한 · 데이터** |
| 09 | **비교표**(엑셀/구식 ERP/오너뷰) | 33 | 도입 절차 · 자료 이관 |
| 10 | **도입 전 · 도입 후** | 34 | **요금제 상세 · 하이퍼링크** |
| 11 | **왜 쉬운가** | | |
| 12 | 챕터 Ⅱ 이런 일이 사라집니다 | | |
| 13~19 | 기능 7장(3단 밴드) | | |

굵은 항목 6개가 도입 검토자용으로 신설된 장표.

## 참고 덱에서 가져온 것

### 아드리엘(내실)
- **기능 장표 = 제목 / |부제| → 고객의 말 2개(구어체·빨강) → 기능 3개 + 캡처**
  한 장만 봐도 "무엇을·어떻게·왜 필요한가·진짜 되는가"가 끝난다.
- **연동 커넥터 그리드를 앞쪽에** — 남의 이름(은행·카드사·국세청)으로 신뢰를 먼저 준다.
- **사용 예시 챕터** — 같은 제품을 용도를 바꿔 반복해서 보여 준다. 화면이 분량의 70%.
- 부제는 파이프(`| … |`)로 감싼다. 고객 인용은 "~해요" 구어체, 빨강은 **아픈 지점에만**.

### 마인드노크(외형)
- 글꼴 한 가족 + 굵기 3단계, **중간 크기(14~16pt)를 안 쓴다** — 제목 23 / 부제 12.5 / 본문 9.8.
- 캡처는 **흰 카드 + 얇은 테두리**에 담고 캡션. 전체 화면 그대로 넣는 장표는 사용 예시뿐.
- 주색 3톤 + 강조 1색, 강조는 한 장에 한 곳. 챕터에도 메시지 문장.
- 안 가져온 것: 짙은 보라 풀밴드·노랑 형광·조직도/예산표(입찰 제안서 전용).

## 만들며 고친 것 (기록)
1. **기능 3열 카드 → 3행 배치.** 제품 조각은 대개 5:1 로 가로가 길어, 3열 카드에 넣으면
   우표처럼 작아지거나 좌우가 잘려 무슨 화면인지 알 수 없었다. 행으로 눕혀 캡처 폭을 8.7"로 줬다.
2. **조각 크롭 시작 x 400 → 350.** 계좌명·거래처명이 왼쪽에서 잘리던 문제.
3. **이미지 왼쪽 정렬.** 가운데 정렬이면 조각 왼쪽 라벨이 가운데로 밀려 시선이 흩어진다.
4. **27번 사용 예시를 거래처 원장 → 손익 계산으로 교체.** 원장은 거래처 미선택 상태라
   "왼쪽에서 거래처를 고르세요" 빈 화면이 찍혔다. 빈 화면을 소개서에 넣지 않는다.
5. 16번 기능 장표에서 같은 조각(f-ledger)이 두 번 쓰이던 것 → 미수 위젯으로 교체.

## 2026-08-20 수정 (사장님 지시)
- **34장을 문의 폼 → 실제 요금제 비교표로 교체.** 회사명·담당자명 같은 입력칸 표기를 없애고
  `billing/page.tsx` 의 실제 요금제 행 12개(구성원·발행·전자계약·통장카드·홈택스·AI 참모·AI 브리핑·
  전자결재/근태/급여/프로젝트·맞춤 UX·기존 시스템 연동·데이터 이관·지원)를 그대로 옮겼습니다.
- **하이퍼링크 4곳.** 표지 `www.owner-view.com`, 34장 `www.owner-view.com` · `요금 안내 →`(/pricing) ·
  `도입 상담 신청 →`(/#partner). PPTX·PDF 어느 쪽에서 눌러도 페이지가 열립니다.
  (`deck2_core.py` 의 `link_text()` / `link_shape()`)


## 2026-08-20 문구 전면 손질 (사장님 지시)
- **02장**: "작은 회사의 운영 소프트웨어" → **"회사 운영 All in one 시스템"**.
  규모(작은 회사)를 강조하지 않는다 — 읽는 분이 자기 회사를 작다고 규정당하는 느낌을 받는다.
- **챕터 어체 통일**: "지금과 무엇이 다릅니까" 같은 딱딱한 물음 → **"기존 ERP와 무엇이 다를까요"**
  · "이런 일이 사라집니다" → **"어떤 일이 줄어들까요"** · "이렇게 씁니다" → **"어떻게 쓰게 될까요"**
  · "도입하기" → **"어떻게 시작할까요"**. 챕터는 질문형 `~할까요` 로 통일.
- **34장**: "회사에 맞게 자랍니다"(제품이 주어) → **"회사에 맞게 사용하세요"**(고객이 주어).
- **그 밖에 AI 스러운 말투 22곳 교정** — 제품이 주어인 문장을 고객이 주어인 문장으로,
  단정형("~합니다")을 안내형("~하시면 됩니다")으로. 예:
  · "자동화의 목적은 …것입니다" → "자동화는 사람을 대신하는 것이 아니라, 판단할 준비를 대신하는 것입니다"
  · "AI 가 대신 눌러 주지 않습니다" → "AI 가 대신 누르지 않습니다. 누르기 전까지의 준비만 대신합니다"
  · "회사 데이터를 아는 AI 에게 물어보십시오" → "회사 자료를 아는 AI 에게 물어보세요"
  · "구식 ERP" → **"기존 ERP"** (비교표 열 이름 포함)
  · "모으는 일이 사라지면, 그 시간은 판단에 쓰입니다" → "자료를 모으는 시간이 줄면, 그만큼 판단할 시간이 늘어납니다"

⚠️ **PPTX 를 직접 고치면 다시 빌드할 때 덮어써집니다.** 문구 수정은 `scripts/deck2_make*.py` 에서 하세요.


## 2026-08-20 이미지 전면 교체 (사장님 지시)
사장님: "이미지들이 다 짤려있는데 · 칸에 딱 한눈에 들어오게 · 23페이지는 은행 로고까지 보여야 ·
그냥 사이드메뉴까지 같이 다 보이게 · 이미지 전부 다 바꿔줘 · 전체화면이 다 나오게"

- **모든 이미지를 조각 → 화면 전체(3200×2000, 사이드바 포함)로 교체.** `deck2-crop.py` 는 이제
  자르지 않고 `cap/v-*.png` 원본을 그대로 복사한다(사용 예시 8 + 기능·설명용 25개).
- **배치는 전부 비율 유지(contain).** cover/crop 방식(`pic_top`)을 소개서에서 걷어냈다 —
  잘린 화면은 무슨 기능인지 알 수 없고 제품처럼 보이지도 않는다.
- **기능 장표(13~19) 구조 변경**: 3행에 조각 3개 → **왼쪽에 기능 설명 3개 + 오른쪽에 대표 화면 1장 크게**.
  조각 셋을 늘어놓으면 각각이 작아져 읽히지 않았다.
- 사용 예시(22~29)는 이미지 영역을 12.23"×5.5" 로 키워 화면이 한눈에 들어온다.


## 2026-08-20 디자인 전면 적용 (사장님 지시)
사장님: "디자인이 너무 밋밋해 · 배경도 없고 · 여백이 많아 빈약해 보인다 ·
같은 텍스트라도 글자 크기·색·강조가 다 다른데 그런 게 빠졌다 · 아이콘·캐릭터도 적극 사용"
(참고 이미지: fs1.PNG 배지+카드, s133.PNG 강조어 색·하이라이트, 111111111113.PNG 아이콘 배지, 1313131.PNG 캐릭터+말풍선)

`deck2_core.py` 에 디자인 시스템을 추가하고 전 장표에 적용:
- **`bg_light()` / `bg_dark()`** — 흰 여백 대신 그라데이션 배경 + 큰 원·대각선 도형(투명도 XML 로 지정).
  밝은 장표는 하늘빛 그라데이션, 표지·챕터는 딥 인디고 + 대각 띠.
- **`pill()`** — 제목 위 알약 배지("회사 운영 All in one", "주요 기능", "자동 연동" 등).
- **`icon_badge()`** — 원형 아이콘 배지 + 번호. Segoe MDL2 Assets 글리프(은행·카드·차트·사람·문서·자물쇠…)
  를 써서 PowerPoint·PDF 모두에서 깨지지 않는다.
- **`check_list()`** — ✔ 체크 목록 상자.
- **`avatar()` / `bubble()`** — 04장 '자주 듣는 이야기'를 캐릭터 + 말풍선으로. 아바타는 도형 조합
  (머리·머리카락·어깨)이라 외부 이미지가 필요 없다.
- **텍스트 강조 체계** — 한 문장 안에서 크기·색을 나눈다. 예: "오너뷰는 회사 운영 **All in one**(27pt 인디고)
  시스템입니다(24pt 검정)". 고객의 말은 아픈 지점만 빨강, 효과는 앰버.

⚠️ 표지 URL 은 하이퍼링크 run 이 테마색(파랑·밑줄)을 강제해 튀었다 → **글자는 흰색으로 두고
링크는 투명 도형에 건다**(`link_shape`).

## 검수
- PPTX 텍스트 421블록 전수 검사 — 실제 거래처명 **0건**. "모티브이노베이션"은 34장 하단
  운영사 표기 1회뿐(의도).
- 데이터는 전부 시연 회사 '오너뷰'. 사업자번호·계좌·카드번호는 형식만 유효한 0 채움.
- 34장 전부 PowerPoint 로 렌더 확인.

## 남은 확인 (사장님)
1. **연동 기관 표기** — 지금은 이름 텍스트 그리드. 실제 로고로 갈지(각 사 CI 사용 조건 확인 필요).
2. **고객 인용** — 지금 문장은 "자주 듣는 이야기" 성격. 실제 고객 문장이 있으면 교체하면 훨씬 세다.
3. **비교표 수위** — 구식 ERP를 깎아내리지 않는 톤으로 썼다. 더 직설적으로 갈지.
4. **공개 가능한 숫자** — 연동 은행·카드사 수(지금 "20+/10+"), 가입 회사 수 등.
5. 33장 "쓰시던 자료는 어떻게 되나요?" 의 이관 범위가 실제 제품 동작과 일치하는지 최종 확인.
