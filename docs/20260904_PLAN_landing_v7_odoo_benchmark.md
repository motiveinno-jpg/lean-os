# 랜딩 v7 기획 — 오두(odoo.com/ko_KR) 벤치마킹 (2026-09-04)

> 사장님: "오두 랜딩페이지 분석. 구성부터 모션, 글씨체, 언어(문법) 등 짜임새 있다. 면밀히 분석해서 오너뷰의
> 새 랜딩페이지를 기획. 지금 랜딩은 그대로 두고 새로. 백프로 똑같이는 안 되고 벤치마킹 정도." → "먼저 기획목업부터"
>
> 목업(데스크톱 1440 · 모바일 390 · 구간별 메모): https://claude.ai/code/artifact/d9d41fa1-2391-47bd-adae-0cdaedaa1a59
> 작업 파일: 이 PC 스크래치 `landing-mock/` (Main.dc.html · Mobile.dc.html · canvas.json). 지금 랜딩(`src/app/page.tsx`, landing-v6)은 손대지 않았다.

## 1. History — 지금 랜딩이 왜 이 모양인가
- v4(2026-07-27) 토스식 해요체 확정 → v5/v6(2026-07-29) 캐러셀→그리드, 공감 구간·사례·가격 미리보기 추가. 히어로는 "헤드라인 2줄 + 부제 + 버튼 2 + 체크 4 + 화면 3장 콜라주".
- 문구·숫자 규칙은 `src/components/landing/content.ts` 머리 주석이 원본(숫자 임의 변경 금지, 해요체, 규모로 대상 안 좁힘, 사례는 실측만).
- 사장님이 오두를 "아주 잘 만든" 예로 든 이유는 **한 화면 안에서 폭(앱 24개)과 깊이(실제 화면 영상)를 같이 보여주는 짜임새**다. 지금 오너뷰 랜딩은 깊이(캡처)는 많은데 폭이 스크롤 여섯 번 뒤(카탈로그)에 있다.

## 2. 오두 실측 (2026-09-04, 1440px, Playwright)
| 항목 | 실측 |
|---|---|
| 길이·구간 | 8,576px · 11구간 · 컨테이너 1,320px |
| 글꼴 | Inter 600(제목) / 300(리드). 64 · 48 · 24 · 20 · 16. 자간 normal |
| 색 | 본문 #111827 · 판 #F3F4F6(흰/회색 교대) · 주색 #714B67 · 버튼 각진 사각(0 radius) |
| 구성 | ①히어로(화면 없음, 손그림 형광+손글씨 가격 메모, 버튼 2) ②언덕 곡선+세미나 알약 ③앱 아이콘 6×4 ④"지치셨나요?→연결하세요!" 두 문장 ⑤전 SAP CEO 인용 ⑥제품 시연 **영상**(브라우저 틀·자동재생·진행바) ⑦화면 조각 콜라주(fade-in) ⑧AI 주장 ⑨철학 카드 2×2(각 버튼) ⑩2,800만 사용자 얼굴 모자이크 ⑪KPMG 후기 ⑫64px CTA+"카드 없어도 됩니다"+손그림 반짝이 |
| 모션 | 사실상 둘 — 시연 영상 자동재생, 스크롤 fade-in(`o_anim_fade_in`). 장식 모션 없음 |
| 문장 | 제목 = 4~6어절 명사구("효율적이고 정확한 업무 처리"). 리드 = 문제 한 문장 → 해결 한 문장, 격식체+느낌표. 버튼 = 동사형("지금 무료로 시작하세요") |

## 3. 자문자답
1. **무엇을 기준으로 판단하나** — 방문자가 첫 두 화면 안에서 ①이게 뭔지 ②얼마나 넓은지 ③실제로 움직이는지를 알아야 한다. 구간 순서는 그 세 질문 순서다.
2. **왜 하나** — 지금 랜딩은 "깊이" 위주라 "이 회사가 뭘 다 하는지"가 늦게 보인다. 폭을 앞으로 당긴다.
3. **반대 경우** — 폭만 보여주면 "얕은 도구 모음"으로 읽힌다 → 바로 다음에 실제 화면 영상으로 깊이를 증명한다. 또 오두식 다색 아이콘·손그림 낙서·격식체를 그대로 가져오면 우리 앱 안 톤(선 아이콘·담백·해요체)과 어긋난다 → 장치만 가져오고 표현은 우리 것.
4. **자동으로 못 푸는 것** — 고객 후기(인터뷰 확보 전엔 자리만), 시연 영상(촬영 필요), 사용자 수(없음 → 실측 4개 수치로 대체).

## 4. 결정 (번호는 brain decisions 이어서 174~)
| # | 규칙 | AS_IS ▶ TO_BE | 적용 | 기존 |
|---|---|---|---|---|
| 174 | 히어로는 화면 없이 말로 시작, 핵심 어구에 브랜드 밑칠이 0.7초에 그려진다(1회) | 헤드라인+화면 3장 ▶ 헤드라인 66/800 + 손글씨 메모(월 39,000원·5명·34메뉴) + 언덕 곡선 | v7 신규 라우트 | v6 유지 |
| 175 | 메뉴 격자 6×4(24/34)를 두 번째 화면에. 아이콘은 브랜드색 선 하나 | 카탈로그가 6스크롤 뒤 ▶ 히어로 직후 | 〃 | CATALOG 재사용 |
| 176 | 세 번째 화면은 **실제 화면 녹화 영상**(무음·자동재생·진행바) | 정지 캡처 ▶ 32초 영상(대시보드→통장→매칭 제안→확인→전표) | 촬영 후 | 캡처는 콜라주에서 계속 씀 |
| 177 | 문장 구조: 문제 한 문장(얇게) → 해결 한 문장(굵게). 해요체 유지 | — | 〃 | content.ts 규칙 유지 |
| 178 | 인용문 자리엔 남의 권위 대신 우리 원칙 "제안은 AI가, 확정은 사람이." | — | 〃 | 자동화 원칙과 동일 |
| 179 | 파란(확정) 버튼은 히어로·마지막 CTA 둘뿐. 나머지는 연한 알약 | 히어로·가격·CTA 등 | 〃 | — |
| 180 | 지어내지 않는다: 후기 `[ ]` 자리, 사용자 수·얼굴 없음, 세미나 대신 실재하는 무료 도구 6종 | — | 〃 | CASES 실측만 |
| 181 | 모션은 4개만(형광 그리기·타일 stagger·영상·콜라주 fade-in). 장식 낙서 없음 | — | 〃 | — |

### 2차 (같은 날, 사장님 지시 3건 반영)
> "해요체 말고 합니다 같은 전문적인 언어. 메인 핵심은 '재고, 회계, 프로젝트, 인사까지 회사 운영은 오너뷰 하나로'. 오두 순서와 너무 똑같으니 꼬아서 오너뷰만의 메뉴 소개 순서로."

| # | 규칙 | AS_IS ▶ TO_BE |
|---|---|---|
| 182 | **v7 문체는 합니다체**(~합니다/~됩니다/~입니다/~하십시오). 느낌표·과장 없음. content.ts 의 해요체 규칙은 v6 전용으로 남긴다 | 해요체 ▶ 합니다체 (177 대체) |
| 183 | 헤드라인 확정: "재고, 회계, 프로젝트, 인사까지 / 회사 운영은 오너뷰 하나로" (밑칠은 '오너뷰 하나로') | — |
| 184 | **순서는 오너뷰 것**: 히어로 → 실제 기록 띠(증명 먼저) → 챕터 01 재고 · 02 회계 · 03 프로젝트 · 04 인사(헤드라인과 같은 순서, 좌우 교대, 앱의 collect-tabs 파란 밑줄 탭) → 05 AI·분석(네 영역 위의 층, 검정 판) → 전체 메뉴 격자(폭은 뒤에) → 원칙+시연 영상 → 도입 조건 카드 → 후기 자리 → CTA. 오두는 폭→깊이→철학→증명, 오너뷰는 증명→깊이→층→폭→동작→조건 | 174·175 의 '격자 두 번째 화면' 철회 |
| 185 | 내비도 재고·회계·프로젝트·인사 네 영역이 앞. 챕터 사실은 실재 기능만(선입선출 원가·채널 주문·홈택스 수집·견적→계약 초안·급여 배치) | — |

### 3차 (같은 날) — 기존 랜딩 내용 전부 폐기, SEO 키워드형으로 재기획
> 사장님: "기존 랜딩 내용에 맞춘 것 같고 오너뷰만의 핵심 기능을 못 뽑았다. 기존 랜딩은 무시하고 아예 새로. 제목만 '회사 운영의 모든 것 올인원 AI ERP, 오너뷰'. '입력은 AI가, 확정은 사람이'는 너무 AI스러운 말투. SEO가 부족 — 'AI 자동입력으로 간편하게. 업무의 최소화' 같은 식. 설명할 곳은 설명, 강조할 곳은 SEO. 커머스 연결·프로젝트 관리·통장카드 자동 수집→전표 생성·손익 분석→세무 신고 등 핵심이 다 빠져 있음. '네 영역 위에서'도 이상."

| # | 규칙 | AS_IS ▶ TO_BE |
|---|---|---|
| 186 | **문장 원칙: 제목은 검색어, 본문은 설명.** H2는 키워드 나열형("근태관리·급여계산·4대보험 자동화"), 본문은 합니다체 설명. 'AI가 ~합니다' 의인화 문장 쓰지 않는다 → '자동으로 들어옵니다 / 계산됩니다 / 만들어집니다' | 182·184 의 챕터 제목 문장형 ▶ 키워드형 |
| 187 | 제목·강조 확정: H1 "회사 운영의 모든 것 / 올인원 AI ERP, 오너뷰", 강조 문장 "AI 자동입력으로 간편하게. 업무의 최소화." | 183 대체 |
| 188 | **핵심 기능 6챕터**(코드 실측): A 통장·카드 자동 수집→전표 자동 생성 / B 손익 분석→부가세·원천세·법인세 신고(tax-filing: 부가세 4시트·원천징수이행상황신고서·전산매체 파일 베타·법인세 체크) / C 이커머스 주문 연동(스마트스토어·쿠팡·11번가·G마켓)→재고·선입선출 원가 / D 프로젝트 관리·견적서·계약서·전자서명 / E 근태관리·급여계산·4대보험 / F AI 경영 브리핑·자금 전망 90일 | 184 의 재고·회계·프로젝트·인사·AI 5챕터 ▶ 6챕터, '네 영역 위에서' 폐기 |
| 189 | 히어로 직후 **한 흐름 다이어그램**: 자동 수집 4 → AI 자동입력(분류·매칭·전표 생성) → 결과 4(전표·장부 / 손익·재무제표 / 세무 신고서 / 급여·명세서) | 실측 기록 띠 ▶ 흐름 그림 (CASES 수치는 랜딩 v7 에서 뺀다) |
| 190 | SEO 장치: 키워드 칩 7개(히어로), 무료 계산기 6종 구간(/tools 유입), 푸터 키워드 문단, H2 전부 검색어형. 메타 title/description 은 구현 시 같은 키워드로 | — |

**버린 안(3차)** — 실측 수치 띠·후기·'제안은 AI가' 원칙 문장: 기존 랜딩의 흔적이라 폐기. 메뉴 24개 격자: 기능 나열보다 6챕터 설명이 검색·이해 모두에 낫다고 판단해 뺐다(필요하면 /features 로).


### 4차 (같은 날) — 모션
> 사장님: "애니메이션·모션이 풍성해서 요즘 프로그램인 게 보였으면. 강조 설명·글자에도 효과. 다 똑같이 반복하면 지루하니 구간마다 다르게."

| # | 규칙 |
|---|---|
| 191 | **구간마다 다른 효과 하나씩, 같은 효과 반복 금지.** ①히어로 줄 올라오기→밑칠 그리기→강조 문장 단어 등장+글자색 흐름→버튼·칩 팝→화면 세워지며 등장(총 2.1초) ②흐름도 점선 흐름·점 이동·가운데 맥동 ③회계 밑줄 자라기+체크 그리기 ④세무 글자색 쓸기+떠 있는 화면 ⑤이커머스 채널 마퀴 ⑥프로젝트 5단계 점등+진행 막대 ⑦급여 체크리스트 순차 완료 ⑧자금 전망 선 그래프 그리기+오늘 점 깜빡임 ⑨AI 3단계 격자 배경 흐름+번호 빛 ⑩계산기 호버 떠오름 ⑪요금 카운트업+테두리 퍼짐 ⑫CTA 빛 덩어리+글자색 흐름+버튼 맥동. 구현: CSS 키프레임 + `data-io`/`.in` IntersectionObserver 하나, `prefers-reduced-motion` 이면 정지. |


### 5차 (같은 날) — 히어로 시연 영상
> 사장님: "맨 처음 오너뷰 메인 이미지를 오두처럼 영상으로 바꾸고 싶다."

| # | 규칙 |
|---|---|
| 192 | **히어로 = 시연 영상.** 장면 ①대시보드(잔고·미수금·오늘 챙길 일) ②통장 연결 → 거래 자동 수집 ③수집·전표 → 증빙 매칭 확인 한 번에 전표 ④손익 현황 반영. 20~30초, 무음, 자동재생·반복, poster = 대시보드 캡처, webm+mp4 3~5MB, 화면 밖이면 정지, 마우스 올리면 일시정지. 목업은 캔버스가 영상 파일을 못 담아 실제 화면 4장 교차 + 커서 이동·클릭 파문 + 장면 자막 + 진행바로 시뮬레이션. 구현 전 촬영(데모 계정, 1440×810). |


### 6차 (같은 날) — B안: 고객 도입 문의 9항목 기반
> 사장님: 드팜므에 보낸 답변 9항목(매출/KPI 대시보드 · 판매채널 연동 · 프로젝트/업무관리 · 일정/캘린더 · 대용량 파일 보관 · 인사/근태/휴가 · 회계/ERP · 사내 매뉴얼 · 메신저)이 "회사에서 필요한 내용이 다 들어가 있는 것 같다. 이걸 토대로 SEO와 랜딩 내용을 기획해 보자. 현재 것은 저장해 두고 새로 목업, 내가 선택하겠다."

| # | 규칙 |
|---|---|
| 193 | **B안 = 요구사항 체크리스트형.** 히어로(제목 고정) → 강조 "따로 쓰던 프로그램 9개, 오너뷰 하나로." → 9개 체크칩(검색어) → 시연 플레이어(대시보드·이커머스·프로젝트·메신저) → "도입 전에 물어보신 아홉 가지, 전부 됩니다" → 9챕터(답변 순서 그대로, H2 검색어형, 답변 원문을 설명으로, **'위치: 메뉴 경로' 칩**) → AI 자동입력(검정 판, 강조 문장 이동) → 요금("프로그램 9개 값이 아니라 회사당 월 39,000원") → 계산기 → CTA. 이지어드민(개발 중)·고객 수치(180GB)는 넣지 않는다. 모션은 A안과 겹치지 않는 9종(KPI 막대·링, 주문 토스트, 단계 이동 카드, 알림 말풍선, 버전 쌓임, 승인 도장, 장부 줄 등장, 탭 순환·타이핑, 말풍선·읽음). |

A안 = 기능 흐름(수집→전표→세무) 설명형, B안 = 고객 질문에 답하는 체크리스트형. 사장님 선택 후 촬영·구현. 혼합(A 히어로 + B 9챕터)도 가능.


### 7차 (같은 날) — B안 2차
> 사장님: "물어본 회사에 대응한다는 표현은 좀 그렇다 → '각각 따로 써서 불편함, 반복되는 업무들, 높아지는 비용들에 오너뷰 하나로 전부 됩니다' 느낌으로. 프로젝트는 촬영·상세페이지에 국한되지 않게 포괄적으로(또는 프로젝트 섹션을 따로 빼 가능한 기능 강조). 캘린더도 국한되지 않게. A안(4차)의 '자동 수집에서 세무 신고까지 한 흐름'과 'AI 자동입력, 이렇게 일합니다'를 B안에 버무려."

| # | 규칙 |
|---|---|
| 194 | 특정 고객·문의에 답하는 투 금지. 히어로 설명·챕터 머리·CTA 전부 "따로 쓰던 프로그램의 반복 업무와 늘어나는 비용을 오너뷰 하나로". 프로젝트 챕터 = 표·칸반·캘린더·간트 + 단계 자유 설정(제조·서비스·마케팅 예) + 견적→계약→서명→입금 + 결재 허브 + 채널 + 템플릿 8종. 캘린더 챕터 = 회의·거래처 방문·납품·신고 마감·휴가·외근 전체 일정 + 할 일·프로젝트 마감. 9챕터 뒤에 A안의 흐름도(리드 "AI 자동입력으로 간편하게. 업무의 최소화.")와 AI 3단계 검정 판을 이어 붙인다. |


### 8차 (같은 날) — **B안 확정 · 실제 페이지 구현** (`/landing-v7`)
> 사장님: "B안이 좋은 것 같다. motion design 스킬이랑 gsap 스킬을 설치한 후 설치한 스킬을 사용하여 제작해줘."

| # | 규칙 |
|---|---|
| 195 | **B안으로 확정.** 목업(아티팩트 224dc619)을 그대로 코드로 옮겼다. 라우트 `/landing-v7`(공개·**noindex**), 현재 랜딩 `/` 는 손대지 않는다. 확정 전환 때 robots 를 빼고 `/` 로 옮기며 canonical 을 바꾼다. |
| 196 | **애니메이션 = GSAP + ScrollTrigger** (스킬 `gsap-core`·`gsap-timeline`·`gsap-scrolltrigger`·`gsap-react`·`gsap-performance` 설치·적용). CSS 키프레임은 두 개만 남긴다 — 글자 그라데이션 흐름(transform 으로 못 만든다), 검정 판 격자 드리프트. |
| 197 | **모션 아키타입 = Corporate** (스킬 `motion-design`). 서명 이징 `power3.out`(등장) / `power2.inOut`(화면 안) / `back.out(1.6~2)`(튀는 것). 길이 팔레트 0.25 · 0.5 · 0.9초. 히어로 시퀀스 총 2.4초. 스태거 총합 500ms 이내, 한 번에 움직이는 요소는 1/3 이하. 층은 항상 주+보조+배경 셋. |
| 198 | 성능·접근성: transform·opacity 만 움직인다(커서도 left/top 대신 x/y 계산). 반복 타임라인은 `ScrollTrigger.onToggle` 로 **화면 밖이면 pause**. `gsap.matchMedia()` 로 `prefers-reduced-motion` 이면 애니메이션을 **아예 만들지 않는다** — CSS 가 아무것도 숨기지 않으므로 최종 상태가 그대로 보인다(JS 실패·크롤러도 동일). 정리는 `useGSAP({ scope })` 가 맡는다. |

**파일**
| 파일 | 역할 |
|---|---|
| `src/app/landing-v7/page.tsx` | 서버 컴포넌트 — metadata(키워드 16개)·JSON-LD(Organization·SoftwareApplication)·noindex |
| `src/components/landing-v7/content.ts` | 문구·데이터 단일 출처. 합니다체, 9섹션, 위치 경로, 요금 사실 |
| `src/components/landing-v7/landing-v7.tsx` | 화면 + GSAP 전부 |
| `src/app/landing-v7.css` | `lp7-` 정지 스타일. 변수는 `.lp7-root` 안에만 — 기존 lp4-/lp5-/lp6- 와 충돌 없음 |
| `src/middleware.ts` | PUBLIC_ROUTES 에 `/landing-v7` 추가 (전역 파일 — 다른 PC 주의) |
| `package.json` | `gsap` `@gsap/react` 추가 (전역 파일) |

**구간별 모션(결정 191 을 코드로)** — 히어로 줄 올라오기·밑칠 그리기(CSS 변수 scaleX)·단어 등장·체크 그리기·화면 세워지기 / 플레이어 장면 교차+커서 이동+클릭 파문+진행바(마우스 올리면 pause) / KPI 막대 자람+달성률 링 감김+숫자 세기 / 새 주문 토스트 교대+채널 마퀴 / 단계 카드 7칸 이동+칩 점등 / 알림 말풍선 pop / 버전 카드 밀려 올라감+저장공간 막대 / 승인 도장 / 장부 줄 순차 기입 / 카테고리 탭 순환+검색어 타이핑 / 말풍선→읽음→파일 / 흐름도 좌우 진입+점 이동+가운데 숨쉬기 / 번호 순차 발광 / 요금 카운트업+테두리 퍼짐 / 계산기 호버 / CTA 빛 덩어리+버튼 맥동.

**남은 것** — ① 히어로 시연 영상 촬영(결정 192). 지금은 실제 화면 4장 교차로 대신하며, `content.ts` 의 `HERO_VIDEO_SRC` 에 경로만 넣으면 `<video>` 로 바뀐다(다른 코드 무수정). ② KPI 현황판 실제 캡처(데모 데이터 채운 뒤 재촬영 — 지금은 위젯으로 표기). ③ 사장님 확정 후 `/` 전환 + noindex 해제 + 사이트맵 등록.

**버린 안** — (a) 오두식 각진 버튼·다색 아이콘: 우리 앱 UI 와 결이 달라 랜딩만 딴 제품처럼 보인다. (b) 손그림 낙서 장식: 담백한 톤과 충돌. (c) 히어로에 캡처 유지: 격자와 영상이 바로 뒤에 오므로 세 번 겹친다.

## 5. 누락 점검
권한·RLS·역할: 해당없음(공개 페이지). 빈상태: 후기 자리 `[ ]`. 경계값: 해당없음. 중복실행: 해당없음. 마감: 해당없음.
기존데이터: content.ts 숫자 그대로. 파급화면: 없음(신규 라우트 `/landing-v7` 로 붙이고 기존 `/` 유지 — 사장님 선택 후 교체). 외부전송: 없음. 인쇄/엑셀: 해당없음.
모바일폭: 390 아트보드 확인(헤드라인 32px, "오너뷰 하나로" nowrap). 되돌리기: 라우트 분리라 `/` 무영향.
법: 후기·수치 허위 금지(표시광고법) — 자리표시자 유지. 경쟁사 실명 비교 없음.

## 6. 다음
1. 사장님 목업 검토 → 방향·문구 확정.
2. 시연 영상 촬영(데모 계정, 1440×900, 32초, 무음).
3. `/landing-v7` 구현: `content.ts` 공유, 새 CSS 네임스페이스 `lp7-`, 기존 landing 파일 무수정.
4. 고객 인터뷰 1건 확보 후 후기 교체.
