# 계정과목 전수 대조 — 다음 세션용 인수인계 (2026-08-12)

사장님 지시: **"이 리스트를 바탕으로 오너뷰 전 계정과목을 맞춰줘."**

## 원본
- `docs/계정과목_리스트_20260811.pdf` — 저장소에 **커밋해 뒀다**
  (`.gitignore` 가 `*.pdf` 를 막고 있어 `git add -f` 로 넣었다. 지우지 말 것)
- ⚠️ 이 PDF 는 글자가 **CID 인코딩**이라 스트림을 풀어도 글자가 안 나온다.
  Node 로 zlib 풀기까지는 되지만 ToUnicode CMap 을 따라가야 한다.
  **가장 빠른 길: `npx playwright` 가 깔려 있으니 브라우저로 PDF 를 열어 화면에서 읽는다**
  (`file:///.../docs/계정과목_리스트_20260811.pdf` 로 열고 페이지별 캡처 → Read 로 읽기).
  또는 사장님께 엑셀/CSV 로 다시 받는 것이 제일 확실하다.

## 지금 상태 (prod, 모티브이노베이션 c361afb9-8a52-4cac-add9-8992f0f7c09c)
- `chart_of_accounts` 에 표준 계정 약 90개가 시드돼 있다 (2026-07-01 작업, `project-chart-of-accounts` 메모리).
- 회사설정 › 회계·세무 › **계정과목** 화면에서 CRUD·검색·'표준 채우기' 가능.

## ⚠️ 코드를 건드리기 전에 반드시 볼 것 — 계정 **코드**는 엔진이 물고 있다
`src/lib/vat-voucher.ts` 의 `STD` 가 코드로 직접 참조한다:
```
bank 101 · ar 108 · vatIn 135 · ap 251 · payable 253 · vatOut 255 · sales 404
```
그리고 `src/lib/account-nature.ts` 가 **코드 구간으로 성격을 판정**한다
(40x 매출 / 45x·5xx 매출원가 / 8xx 판관비 / 9xx 영업외 / 998 법인세).

→ 코드를 바꾸면 **전표 자동분개·손익계산서 분류·재무상태표 성격**이 동시에 흔들린다.
   작업 순서: ①PDF 를 표로 옮긴다 ②지금 DB 와 diff ③**코드가 바뀌는 것**과 이름만 바뀌는 것을 가른다
   ④코드가 바뀌면 위 두 파일 + `journal_lines` 기존 데이터 영향까지 확인 ⑤그다음 반영.

## 이번 세션에서 함께 확인된 사실 (배경)
- 확정 전표 42건은 전부 매입(51)이고, 통장에서 만든 수금·지급 전표가 많다.
  그래서 재무상태표에서 108 외상매출금·251 외상매입금이 음수다(계산 오류 아님 — 화면이 설명하게 해 둠).
- 재무제표는 **확정 전표만** 읽는다(`src/lib/journal-reports.ts`). 드릴다운도 2026-08-12 부터 같은 원천.
