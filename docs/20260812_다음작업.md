# 다음 작업 — 2026-08-12 이어서

## 1. ✅ 완료 — 카드 '3. 일반 → 일반전표' (3b1ac2b4)

## 2. ⏭ CODEF 카드 응답에서 **가맹점 사업자번호** 받아오기
- 증상: `card_transactions.raw_data->>'merchantBusinessNo'` 가 **2,771건 중 611건(22%)** 에만 있다.
  카드사별로 갈린다 — **BC카드 1,009건 0개 · 롯데카드 1,006건 0개**, 반면 'BC카드 9956' 은 37건 중 25개.
- 이 번호가 없으면 `merchant_tax_types`(구분: 법인·일반·간이·면세)를 못 채운다.
  구조·화면·국세청 조회는 **이미 다 만들어 뒀다**(5cf05c9d) — 번호만 들어오면 저절로 채워진다.
- 볼 곳: `supabase/functions/codef-sync` 의 카드 분기. CODEF 응답 필드명이 상품/카드사별로 다를 수 있다
  (`resMemberStoreCorpNo`, `resMemberStoreNo` 등). 지금 저장하는 필드와 실제 응답을 대조할 것.
- ⚠️ 홈택스 분기(hometax/cash-receipt)는 **BLOCKED** — 건드리지 말 것(CODEF 운영팀 답변 대기).

## 3. ⏭ 계정과목 전수 대조
- `docs/20260812_계정과목_전수대조.md` 참조. 엑셀을 받으면 그것으로, 없으면 PDF 를 브라우저로 읽는다.

## 이번 세션에서 미뤄 둔 것
- 랜딩 스크린샷 재캡처 — **메뉴 확정 후**. `/demo` 목업의 옛 이름(자산관리·자동 분류·세금계산서)을
  먼저 고치고 재캡처. `f-*-b-*.png`(before 이미지)는 일부러 옛 모습이라 건드리지 말 것.
