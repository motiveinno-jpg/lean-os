# 대시보드 v3 기획 — "회사 요약 보고서": 위젯 벽 대신 읽는 순서가 있는 한 장 (2026-09-03, 사장님 "오너뷰만의 특색")

목업(동작): 아티팩트 `dashboard-v3-report`. 결정 번호 158~. v2(결정 149~157, 같은 날 1·2단계 배포)의 부품은 그대로 쓴다.

## History
- 2026-07-15 카탈로그 위젯 격자 → 2026-08-19 층 3(신호·챙길 것·격자) → 2026-09-03 v2 1·2단계(신호 띠·체크리스트·오늘 한눈·격자 규율).
- v2 배포 직후 사장님: "내용이 많고 위젯이 많으니 정신사나움. 차라리 회사 전체 요약 보고서 같은 느낌으로. 권한에 따라 보이는 화면은 다르겠지만."
- 진단: 디자인 마감이 아니라 **밀도**. 위젯 13개 = 제목·숫자·링크를 각자 가진 카드 13장이 읽는 순서 없이 벽처럼 선다. 위젯 형식은 "각자 골라 본다"에는 맞고 "아침에 회사 상태를 파악한다"에는 안 맞는다.

## 자문자답
- **기준**: 아침에 열어 위에서 아래로 **한 번** 읽으면 회사 상태와 오늘 할 일이 끝나는가. 좌우로 눈이 움직이면 실패.
- **왜 보고서인가**: 대표가 매일 받고 싶은 것은 "위젯 모음"이 아니라 "비서가 정리한 한 장". 오너뷰의 자료(전표·통장·근태·프로젝트)가 이미 다 있으니 문장으로 엮을 수 있다 — 이게 다른 툴에 없는 특색.
- **반대 경우**: 위젯을 골라 보고 싶은 사람 → 보고서 아래 **부록**에 접어 둔다(없애지 않는다). 숫자만 빨리 보고 싶다 → 2절 숫자 띠가 그 역할. 직원은 회사 돈을 볼 수 없다 → 권한이 절을 지운다.
- **자동으로 못 푸는 것**: 절 문장의 "판단"(위험하다/괜찮다) — 규칙이 아니라 사람이 정한 임계값(신호 톤과 같은 함수)으로만 말하고, 그 밖의 해석은 결론 한 줄(AI 제안 표기)에만 둔다. 문장이 틀렸을 때 원인을 찾을 수 있게 **절마다 출처 화면 링크**.

## 결정
**158 — 첫 화면 = 보고서 한 장, 위젯은 부록**
- 규칙: 대시보드는 한 열(최대 폭 920px) 문서. 절 순서 고정: ①결론 ②회사 상태(숫자 띠) ③오늘 챙길 것 ④자금 ⑤매출·미수 ⑥업무 ⑦사람 ⑧부록(위젯). 카드 상자는 절 안에서 쓰지 않는다 — 절 제목·문장·작은 표·링크만.
- AS_IS 위젯 격자 13개 ▶ TO_BE 보고서 7절 + 부록(접힘). 적용 시점: 목업 승인 후 1단계. 기존 데이터: 위젯 배치·켜 둔 목록(user_preferences)은 부록이 그대로 읽는다(버리지 않음).
- 버린 안: 위젯 수만 줄이기 — 카드 벽의 성격은 그대로라 밀도 문제가 안 풀린다.

**159 — 절 문장은 규칙, 결론 한 줄만 AI**
- 각 절의 문장은 숫자 조합 규칙(lib/biz-summary 의 톤 판정과 같은 임계값)으로 만든다. 예: "통장 잔액 6,219만원, 30일 뒤 4,534만원(−1,686만원). 고정비 기준 33개월 운영 가능." 판단어(안정·주의·위험)는 톤에서만.
- 결론 한 줄과 오늘 챙길 것만 AI 제안(기존 브리핑 로직·권한·30분 쿨타임 그대로).
- 버린 안: 절 문장까지 AI — 매일 문장이 흔들리고 틀렸을 때 원인을 못 찾는다.

**160 — 개인별 권한이 절을 켜고 끈다 (역할 프리셋 없음, 새 권한 없음) — 2026-09-03 사장님 "대표/회계/직원 말고 개개인 권한별로" 확인**
- 절 ↔ 권한 대응표: 결론·챙길 것 = /dashboard:briefing / 회사 상태·자금 = /dashboard:finance(+/bank) / 매출·미수 표 = /partners/ledger / 세금 = /tax-invoices 또는 /finance/tax-filing / 업무 절의 결재 = /approvals, 프로젝트 = /projecthub, 재고 = /inventory/stock / 사람 = /employees 또는 /attendance. 구성원 권한 설정(member_permissions)을 그대로 읽는다 — 같은 회사라도 사람마다 절 구성이 다르다.
- 목업의 '대표 예/회계 예/직원 예' 버튼은 체크박스 묶음 단축키일 뿐, 실제 화면에는 없다.
- 자금·매출·미수 절 = /dashboard:finance + 그 메뉴 권한(위젯 게이트와 동일). 업무 절 = 결재·프로젝트·재고 권한 있는 부분만. 사람 절 = /employees 또는 /attendance. 결론·챙길 것 = /dashboard:briefing.
- 직원에게는 ①결론(내 일 기준) ③오늘 챙길 것(내 것) ⑥업무(내 담당·결재) ⑦사람(오늘 근무) + 부록(직원 위젯).
- AS_IS 프리셋(대표/회계/직원)이 위젯 묶음을 정함 ▶ TO_BE 프리셋 없음, 권한이 곧 분량. 프리셋 UI 삭제.

**161 — 부록**
- 보고서 맨 아래 "부록 — 위젯 N개" 한 줄, 펼치면 v2 격자(크기 조절 없음·이동 자유·빈 위젯 접기)가 그대로. 보기 설정은 부록 안에서만.
- 펼침 상태는 기기에 기억(localStorage) — 조회값이 아니라 보기 상태.

**162 — 문서 타이포**
- 날짜 제목 26px, 절 제목 15px + 위 얇은 실선, 본문 14px/1.7, 표 13px. 색은 톤 글자(주의 주황·위험 빨강)와 링크 보라만. 숫자는 tabular.

## 누락 점검
권한: 결정 160 · RLS 해당없음(읽기만, 기존 쿼리) · 역할: 위 · 빈 상태: 절이 비면 "해당 없음" 한 줄(절을 숨기지 않는다 — 없다는 것도 정보) · 경계값: 음수·D-0·0건 문구 규칙 표로 · 중복 실행: 결론 다시 생성 쿨타임 그대로 · 마감 해당없음 · 기존 데이터: 부록이 옛 배치 승계 · 파급: dashboard/page.tsx(첫 화면 교체), 격자·위젯은 부록으로 이동, 신호·브리핑·오늘 한눈 재사용 · 외부 전송 해당없음 · 인쇄: 보고서라 **인쇄 CSS 1장**(A4) 포함 · 모바일: 한 열이라 그대로 · 되돌리기: 부록 펼침 토글.

## 단계
1) 보고서 골격 + ①②③(기존 부품) + ⑧부록(기존 격자) — 반나절  2) ④~⑦ 절 문장 규칙(lib/report-lines.ts) + 표 — 하루  3) 권한 분기·인쇄 CSS·모바일 — 반나절

## 반영 (2026-09-03 — 사장님 "진행해줘")
- 1·2·3단계를 한 커밋으로: components/morning-report.tsx(골격·절·부록·인쇄 CSS) + lib/report-lines.ts(절 문장 규칙) + MorningBrief variant(lead/checklist) + page 권한 8종 → perm.
- 격자에서 신호·챙길 것·오늘 한눈 위젯 제거(보고서 본문), dashboard-glance.tsx 삭제, 프리셋 UI 제거(presets=[]). 배치 키·켜 둔 목록은 부록이 승계.
- 실측(모티브 대표 계정): 절 01~06 실데이터 문장, 부록 펼치기 → 격자 10위젯·잘림 0.
- 남은 것: 절 문장 경계값 표(0건·음수·D-0 문구) 문서화 · 사람 절 52시간 초과(주간 집계 필요) · 직원 계정 실측(권한 적은 사람의 절 구성) · 인쇄 실물 확인.

## 추가 결정 (2026-09-03 저녁 — 사장님 "C가 좋은데 그래프로 더 풍성하게")
**163 — 폭: C안** 보고서 상자 920 → 1280. 자금·매출·미수·업무 절은 **문장+표 왼쪽(최대 720px, 한 줄 50~65자) / 그래프 오른쪽(400px)** 두 열. 신호 띠·챙길 것·사람은 전폭. 1,100px 아래는 한 열. 인쇄는 한 열.
**164 — 절마다 그래프 최대 2개, 문장이 말한 숫자만** (dataviz 규칙 적용·색약 검증 통과 팔레트: 보라 #4F46E5 / 둘째 계열 청록 #0D9488, 다크 #7C7FF2/#10A395):
- 01 회사 상태: 숫자 아래 통장 잔액 30일 선 + 월 손익 6개월 막대(음수 빨강)
- 03 자금: 30일 잔액 전망 곡선(예정 큰 출금일 세로선·툴팁) + 낼 돈 구성 분절 막대(급여·정기 지출·대출)
- 04 매출·미수: 매출·비용 6개월 두 계열 막대(범례) + 받을 돈 지연 기간별 가로 막대(0–30/31–90/91–180/180+, 91일+ 주의·180일+ 위험)
- 05 업무: 프로젝트 줄 마감 상태 분절 막대(지남·이번 주·그 뒤·완료)
- 06 사람: 오늘 근태 분절 막대(근무중·퇴근·휴가·기록 없음)
- 규칙: 막대 ≤24px·끝 둥글게·선 2px·격자 하나·값은 끝점/최댓값만 직접 표기·나머지 툴팁. 상태색(주황·빨강)은 상태에만, 계열 색으로 쓰지 않는다. 표는 그래프가 대체하지 않는다.
- 자료: 잔액 30일 = bank_transactions 일별 누계, 손익 6개월 = biz-summary pnl.series, 전망 = cash-pulse 일별, 낼 돈 구성 = arap(salary·recurring·loanMonthly), 매출·비용 6개월 = 손익 월별, 미수 연령 = 미수 목록 oldestDays 버킷, 프로젝트 = project_items due_date/status, 근태 = 사람 절 집계.
- 목업: 아티팩트 dashboard-v3-rich(그래프 보기 토글). 구현 = 세 번째 커밋(rep-chart SVG 부품 1개 + 절별 데이터 5쿼리, 하루).

## 개정 (2026-09-03 밤 — 사장님 "그래프 디자인·위치가 다 똑같은 형식, 내용에 안 맞는 그래프를 억지로 쓴 느낌, 형식이 굳어 촌스러움" + 타사 '대시보드 보고서' 도움말 예시 공유)
**163 개정 — 절마다 배치가 다르다(틀 금지)**: 자금 = 문장 → 전폭 그림 1 → 문장 → 작은 표 2 / 매출·미수 = 두 열(문장+인라인 선+눈금띠 | 미수 상위 표) / 업무·사람 = 한 열 글(표 없음, 링크로) / 회사 상태·챙길 것 = 그대로. "문장→그래프→오른쪽 표" 반복 금지.
**164 개정 — 그림은 모양이 정보인 곳에만 3개**: ①30일 잔액 전망 곡선(전폭, 급여·세금·정기 지출 주석, 최저점 표시, 그림 1 캡션) ②매출 추세 인라인 선(문장 안 64×14) ③미수 지연 눈금띠(한 줄, 오래될수록 진하게) + 사람 13점. **뺀 것**: 신호 아래 스파크라인, 손익 6개월 막대, 낼 돈 구성 막대, 매출·비용 두 계열 막대, 프로젝트 마감 막대, 근태 막대(문장으로 충분). 둘째 계열 색(청록)도 삭제 — 색은 보라 하나.
**165 — 포함 규칙을 보고서에 적는다**(타사 예시에서 가져온 것: "대시보드에 있는 프로젝트만 보고서에 기여" 식의 명시): 결론 아래 "이 보고서가 읽은 것" 띠(확정 전표·통장 N계좌 수집 시각·카드·활성 프로젝트 N(보관·시연 제외)·재직 N·승인 휴가 + 범위 바꾸기 링크), 절 끝에 "포함/제외" 한 줄(매출·미수·업무·사람), 그림 캡션에 반영 범위. 미분류·초안·시연·퇴사자는 항상 제외.
- 목업: 아티팩트 dashboard-v3-editorial. 구현 시 report-lines 에 '최저점 날짜' 계산(cash-pulse 일별) 추가, 시연 프로젝트 제외 필터(deals.name like '[시연]%' 는 모티브 한정 — company 시연 플래그 검토).

## E안 — 판형 + 보고서 (2026-09-03 밤, 사장님 참고 이미지 6·7.PNG + "보고서스럽게, 두꺼우면 촌스럽다, 100명이면 점 100개냐")
**163 재개정 — 판형은 7.PNG(Lethe), 말투는 보고서**: 얇은 선 한 장 안에 KPI 5칸(첫 칸은 채우지 않고 윗선 2px만) → 절마다 왼쪽 150px 절 이름 열 + 오른쪽 본문. 절은 01 챙길 것(체크리스트) / 02 자금(문장 → 얇은 전망선 그림 1 → 캡션) / 03 매출·미수(두 열: 문장+그림 2 | 미수 상위 표) / 04 업무(세 칸 숫자+한 문장) / 05 사람 / 06 최근 처리 표. 결론 한 줄은 제목 밑.
**164 재개정 — 두께**: 선 1.5px, 비교 계열은 1px 점선 회색, 격자는 hair 색, 점은 r=3 흰 속. 막대 차트 없음. 숫자 굵기 600 이하, KPI 21px. 색은 보라 하나 + 상태색 글자.
**166 — 규모 규칙(미래 데이터)**: 같은 칸이 데이터 크기에 따라 모양을 바꾼다. 목업에 13명·12곳 / 120명·340곳 / 800명·2,100곳 시뮬레이션.
- 사람: ≤20명 점 하나=한 사람(hover 이름) · 21~200명 비율 띠 한 줄 + 부서별 표(상위 4 + 외 N), 이름은 예외(52시간·기록 없음)만 링크 · >200명 사업장 단위 표 + 어제 대비 변화, 이름은 화면에 안 나옴(근태 › 예외로).
- 미수: ≤20곳 상위 5 표 + "N곳 전부" · 21~500곳 표 + 연령 띠(30/90/180/180+) + "상위 5가 전체의 N%" · >500곳 연령 띠가 표보다 먼저, 표는 90일 초과 상위만.
- 통장: ≤5계좌 계좌별 · 그 이상 은행별 합계 + 이상 계좌만. 프로젝트: 마감 지남·정체는 건수 + 상위 3 링크, 나머지 "외 N".
- 임계값은 기본값(회사설정에서 바꿀 수 있게), 규칙 문장은 절 끝 `.rule` 에 그대로 적는다.
- 목업 dashboard-v3-report-panel.html. 구현 시 report-lines 에 scale 분기 + 부서/사업장 집계 쿼리(employees.department·work_site) 필요.

## 반영 — E안 배포 (2026-09-03 밤, 사장님 "일단 이 형태로 배포. 아침 보고서보다 데일리 보고서 같은 명칭")
- 이름: **데일리 보고서**(제목·주석·brain). 파일명 morning-report.tsx / MorningBrief 는 그대로(이름 바꾸기 = 파급만 크고 얻는 것 없음).
- morning-report.tsx 전면 교체: `.rep-sheet` 한 장 안에 KPI 5칸(오늘 잔액·이달 매출·이달 손익·미수·30일 안에 낼 돈, biz-summary) → 절 = 왼쪽 150px 이름 열 + 본문.
  01 챙길 것 / 02 자금(fundsLines + **그림 1** ForecastFig = fetchOutlook(30)+buildCurve, 자금 전망 화면과 같은 키 · 큰 지출 상위 3일 주석 · 최저점 빨간 점 · 세금·납부 한 줄) /
  03 매출·미수(두 열: salesLines + **그림 2** SalesFig(biz-summary series 에 revenue·cost 추가) + 연령 띠 | 미수 상위 5 표 + 각주) / 04 업무(세 칸: 결재·프로젝트(마감 지난 상위 3 링크)·재고(부족 상위 3)) /
  05 사람(규모 규칙: 점 / 띠+부서표 / 부서표) / 06 최근 처리(approval_requests 최신 + 계산서) → 부록 → "이 보고서가 읽은 것".
- 결정 166 구현: report-lines `SCALE`·`peopleShape`·`recvShape`·`ageBucket`. 모티브 실데이터 = 미수 567곳 → `stripFirst`(연령 띠 먼저, 표는 90일 초과 상위만) · 재직 13명 → 점. 사업장(work_site) 열은 없어 >200명도 부서 표(어제 대비는 후속).
- 지운 것: 회사 상태 절(신호 6칸) → KPI 띠가 대신. dashboard-signals.tsx·.dash-sig* CSS·workLines 삭제(고아).
- 실측(로컬 prod 화면, 1400px): 제목 데일리 보고서, KPI 5, 절 6, 그림 1 743×147 · 그림 2 294×93, 문서 가로 스크롤 없음, 보고서 본문 잘림 0(잘림 22는 전부 부록 위젯·브리핑 why 줄 = 기존).
- 남은 것: 사람 절 어제 대비(>200명)·52시간 초과, 인쇄 실물, 직원 계정 실측, KPI 조 단위, 챙길 것 줄 수 상한.

## G안 — 카드형 (2026-09-03 밤, 사장님 참고 이미지 6~11.PNG "이미지별 공통점을 찾아 목업에 반영" → "G안으로 가자, 목업 배경색을 오너뷰 배경색으로, 먼저 모티브에만")
**167 — 판형은 G안(카드 격자), 게이트는 feature_rollout 'dashboard_g'**: 모티브(c361afb9) 행만 넣었다. 켜진 회사는 daily-report.tsx(G안), 꺼진 회사는 morning-report.tsx(E안). 사장님 확인 뒤 company_id null 행으로 전체.
- 여섯 이미지의 공통 11가지 → 반영: ①연회색 바탕(--bg #F4F5F9 / 다크 #0F1116, html[data-dg] 로 게이트 회사만) + 흰 카드 12px ②KPI 5장 = 색 원 아이콘 + 숫자 + 비교 문구 + 스파크라인(잔액 30일은 오늘 잔액에서 일별 순증감을 거꾸로 뺀 값, 매출·손익은 biz-summary 6개월; 미수는 연령 미니 띠, 낼 돈은 구성 미니 띠 — 이력이 없는 값에 억지 선을 긋지 않는다) ③넓은 시계열(자금 전망 면 채움 + 마우스 툴팁: 그날 잔액·오가는 돈) ④표 카드 = 검색 + 상태 알약 + 쪽 6줄 ⑤도넛(미수 상위 3 + 기타) ⑥누적 막대(연령) ⑦아바타 순위(미수 상위) ⑧활동 타임라인(최근 처리 5) ⑨묶음 막대(매출·비용 6개월) ⑩주색 하나 + 상태색 ⑪카드 머리 제목 좌·컨트롤 우.
- 오너뷰 것: AI 결론 카드(맨 위), 챙길 것 체크리스트 + 업무 상태 4줄, 사람 점/띠 규모 규칙(결정 166), 카드 각주(포함 범위), 바닥 "이 화면이 읽은 것", 권한별 카드 숨김.
- 숫자는 lib/daily-report-data.ts 한 곳 — E안도 같은 훅으로 옮겼다(두 화면이 다른 숫자를 보이면 안 된다).
- 기간 토글(주/분기, 60/90일)은 목업에만 있고 화면엔 '월'·'30일' 한 칸만 — 다른 기간은 각 상세 화면 링크로. 날짜 이동(어제 보고서)은 후속.
- 실측(로컬 prod, 모티브): html[data-dg]=1, body 배경 rgb(244,245,249), KPI 5·카드 15, 스파크라인 3(매출·손익·잔액), 도넛·누적 막대 렌더, 미수 567곳 → '90일 넘긴 미수 상위' 갈래, 가로 스크롤 없음, 보고서 잘림 0(잘림 21은 전부 부록 위젯·브리핑 why = 기존).
- 남은 것: 날짜 이동·스냅샷, 잔액 30일 추세를 통장 잔액 이력 표로(지금은 역산), 최근 활동에 행위자 이름(approval_requests.requester_id 조인), 전체 배포(company_id null), E안 삭제.

## 결정 168 — G안 후속: 스냅샷 표로 날짜 이동 + 잔액 이력, 자금 전망 30·60·90일, 행위자 이름 (2026-09-04, 사장님 "남은 것 진행")
- **daily_report_snapshots(company_id, day, payload jsonb)** — 보고서를 연 날 화면이 읽은 숫자를 30분 간격 상한으로 upsert. users FK 없음(2026-09-03 장애 교훈). RLS = get_my_company_id(). 저장은 게이트 켜진 회사(snapshot 옵션 true = G안)만 — E안 회사는 데이터를 만들지 않는다.
- **날짜 이동** ‹ › + 오늘: 지난 날은 저장본만 읽고 질의는 하지 않는다(useDailyReportData opts.day). 저장본 없으면 "열어 본 날만 저장됩니다". 결론·챙길 것은 오늘만(지난 날은 안내 한 줄). 30·60·90 토글은 지난 날 비활성.
- **잔액 30일 추세** = 스냅샷 balance 가 있는 날은 그 값, 없는 날은 오늘 잔액에서 일별 순증감 역산. 날이 쌓일수록 정확해진다.
- **행위자** = approval_requests.requester_id → users.name(같은 회사만 읽힘). 계산서는 작성자 칸이 없어 비움.
- **자금 전망 30·60·90** = fetchOutlook(horizon) 같은 함수·키(["cash-outlook", company, horizon]) — 자금 전망 화면과 캐시를 나눠 쓴다. x축 눈금은 horizon/6 일 간격.
- 버그·교훈: `new Date("YYYY-MM-DDT00:00:00").toISOString()` 은 KST 자정이 전날 15:00Z 라 **하루가 밀린다** → 첫 실측에서 "오늘"이 어제 저장본 모드로 잡혀 카드가 전부 "읽는 중". `shiftDay()`(UTC 산술)로 통일. 같은 패턴이 proj weekStr 등 기존 코드에도 있다(경계값 하루 오차, 후속).
- 실측(로컬 prod 모티브 9/4): 오늘 날짜 정상, 스냅샷 1행 저장, 90일 토글 시 x축 9/3~12/2·"90일 뒤 잔액 +4,140만", ‹ 로 9/3 이동 시 "저장된 보고서가 없습니다", 오늘 복귀, 타임라인·표에 이경원·양정훈 이름.
- 안 한 것: 매출·비용 주/분기 토글(6개월 월 계열뿐 — 분기 2개는 의미 없음, 상세 화면 링크로), 전체 배포·E안 삭제(사장님 확인 뒤).

## 결정 169 — 자금 전망 기본 그림 = 폭포 (2026-09-04, 사장님 "2번이 좋은 것 같아" — fund-charts 목업 네 안 중)
- **규칙**: 지금 잔액(주색) → 사건 막대(나가는 돈 빨강·들어오는 돈 초록, 점선으로 잇는다) → N일 뒤 잔액(주색). 사건은 cash-outlook curve 의 items(급여·정기 지출·대출·세금·매출 입금·매입 지급·계약 회차·결재 대기). **9개를 넘으면 큰 것 8개만 두고 나머지는 '기타 N건'**(툴팁에 이름 5개까지). 막대 `<title>` = 날짜·종류·이름·근거·금액.
- 선(AreaFig)은 "날짜별" 토글로 남긴다 — 언제 빠지는지는 선이, 무엇 때문인지는 폭포가. 30·60·90 토글은 둘 다에 적용.
- 머리 숫자는 그림과 같은 계산(curve.end). 실측에서 cash-pulse forecast30(4,220만)과 outlook curve.end(1.3억)가 달라 폭포 끝 막대와 어긋났다 → curve 가 있으면 curve.end 로 통일. (두 계산기의 차이 자체는 별건: cash-pulse 는 계약 회차·미수 입금을 안 본다 — backlog)
- AS_IS 면 선 ▶ TO_BE 폭포 기본. 적용 = 모티브(dashboard_g). 기존 데이터 처리 = 없음(스냅샷 curve 그대로 폭포로 그려진다).
- 버린 안: ① 계단선(사장님 미선택, 날짜별 토글에 계단 대신 면 선 유지), ③ 주간 막대, ④ 안전선(회사설정 값 필요).
- 실측(모티브 9/4): 막대 11개(지금 + 8 + 기타 7건 + 30일 뒤), 눈금 0·5천만·1억, 툴팁 근거 문구, 토글 전환 OK.

