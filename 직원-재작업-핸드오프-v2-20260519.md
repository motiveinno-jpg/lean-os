# 직원 재작업 핸드오프 v2 (2026-05-19) — 1차 처리 후 "여전히 안 됨" 건

> 1차(핸드오프 v1) 13커밋 배포 후, 직원이 Notion 완료기록 각 카드 `🔧 재작업·추가 요구사항`에
> 재검수 결과를 다시 적음. 아래는 **직원이 "아직 안 된다"고 후속 작성한 것 + 우리가 누락한 요청 + DB 적용 대기**.
> 출처: Notion "모티브이노베이션_오너뷰" › ✅ 완료 기록 각 카드 🔧 토글 (verbatim).
> 직원 검수 체크박스 ☑ 한 것은 《통장 자동이체 '자동' 표기》《대시보드 4요약 위젯》 **2건뿐**, 나머지는 ☐.
> ⚠️ 직원이 "거짓말치지마세요😐" 라고 적음 — 처리 답신만 달고 실제 안 고쳐진 게 있었다는 신호. **이번엔 항목별 실제 검증 증거를 함께 회신할 것.**

---

## 🔴 P0 — 직원 강한 불만 / 1차 처리가 실제로 안 된 것

### V1. 급여이력이 안 없어짐 (1차 c353c7f 가 실제 미해결)
- **출처 카드**: 급여 배치 — 지난달 명세 복사 선택
- **직원 후속(verbatim)**: "급여이력 안사라짐" / "이건 왜 안해줘요 급여이력 없애주세요"
- **경위**: v1에서 c353c7f 가 '급여이력'+'급여명세'를 '급여' 단일 탭 + 내부 세그먼트(이력↔명세)로 통합했고, 본체 QA 'ㄴ급여이력 없애기'를 완료 처리함. 그러나 **직원은 급여이력이 여전히 보인다고 함** — 세그먼트로 남긴 게 문제. 직원 의도는 "급여이력을 세그먼트로 남기지 말고 **아예 제거**".
- **라우팅**: hr-agent — `src/app/(app)/employees/page.tsx` 급여 탭 내부 세그먼트(SalaryTab 이력 뷰)
- **할 것**: 급여 탭에서 '이력' 세그먼트/뷰 자체를 제거(명세만 남김). `?tab=salary` 딥링크가 이력으로 가던 매핑도 명세로. 직원/관리자 양쪽 확인.
- **하지 말 것**: 급여 명세(payroll) 기능 회귀 금지. 관리자 전용 급여 관리뷰는 별도 — 직원 화면에서만 이력 제거인지 관리자도인지 확인(아래 불확실).
- **완료 정의**: 직원·관리자 화면 어디에도 '급여이력' 진입/탭/세그먼트가 없음. 직원 재검수 체크 통과.
- **불확실**: "없애기"가 직원 화면만인지 관리자 포함인지 직원 미명시. 일단 직원 화면 완전 제거 + 관리자도 이력 탭 노출 안 함으로 진행하되, 회신 시 명시.

### V2. 딜 담당자 지정 알림이 실제로 안 옴 (1차 96da17a 가 실제 미동작)
- **출처 카드**: 딜 담당자 추가·제거
- **직원 후속(verbatim)**: "알림안옵니다"
- **경위**: v1 96da17a 가 "담당자 지정 시 알림 발송"을 반영했다 했으나 **직원은 알림이 안 온다고 함**. 발송 트리거/수신 경로 어딘가 미동작.
- **라우팅**: growth-agent(딜 담당자 지정 이벤트) + ops-agent(알림 발송/표시). 알림 테이블·전송 채널 확인 → 다중 도메인 가능(L).
- **할 것**: 담당자 지정 시 알림 INSERT/발송이 실제 실행되는지, 대상 사용자에게 표시되는지 end-to-end 재현. 누락 지점(이벤트 미발화 / 잘못된 수신자 / 알림센터 미표시) 규명·수정.
- **완료 정의**: 담당자 지정 → 그 사용자 알림센터에 "〇〇 프로젝트 담당자로 지정되었습니다" 실제 도착(재현 캡처).

### V3. 거짓 ✅ 신호 — 배치#2 3건 실제 검증 + 누락 1건 구현
- **출처 카드**: 배치 #2 (회계 분석 허브)
- **직원 후속(verbatim)**: "거짓말치지마세요😐"
- **경위**: v1에서 R1(인원별지출 재직월 제외)·R7(4화면 뒤로가기)·R8(빠른위젯 이동) ✅ 답신함. 직원이 "거짓말"이라 함 = **셋 중 하나 이상이 prod에서 실제 동작 안 함**. + 직원의 **4번째 요청을 우리가 통째로 누락**: "스크롤 내렸을때도 재무상태표/손익계산서/고정비변동비/인원별지출 제목은 상단에 고정되게" (답신 없음).
- **라우팅**: ops-agent — `src/app/(app)/reports/` (인원별지출 9103d9c · 뒤로가기 f0518f5) + `src/app/(app)/dashboard/page.tsx` (빠른위젯 dec4531)
- **할 것**:
  1. R1·R7·R8 각각 prod 실제 동작 재검증. 안 되는 것 수정. **항목별 검증 증거(실데이터 스크린/수치)를 회신** — 직원이 "거짓말"이라 했으므로 말이 아니라 증거로.
  2. 누락 구현: 분석 4개 하위 페이지에서 스크롤 시 섹션 제목(재무상태표/손익계산서/고정비변동비/인원별지출) **상단 sticky 고정**.
- **완료 정의**: R1/R7/R8 동작 증거 제시 + 4화면 제목 sticky 동작. 직원 재검수 체크.

---

## 🟠 P1 — 1차 처리가 부분/오동작, 새 버그 제기

### V4. 내 할일 위젯 — 관리자 모드에서 안 됨 (1차 9032751 부분만)
- **출처 카드**: 내 할일 대시보드 위젯
- **직원 후속(verbatim)**: "직원에서는 되는데 관리자모드에서는 안됨"
- **경위**: v1 9032751 가 직원 대시보드에 '내 할일' 노출 → 직원 OK, **관리자 모드에선 미표시/미동작**.
- **라우팅**: ops-agent — `src/app/(app)/dashboard/page.tsx` 역할별 위젯 노출
- **할 것**: 관리자 모드 대시보드에도 '내 할일' 위젯 노출·동작. 역할 분기에서 admin 누락분 보강.
- **완료 정의**: 직원·관리자 양쪽 대시보드에서 '내 할일'이 보이고 본인 할일 표출.

### V5. 정기결제내역/고정비 반영 안 됨 (R15 와 별개 신규 버그)
- **출처 카드**: 카드 '정기결제내역' 명칭 변경
- **직원 후속(verbatim)**: "고정비로 체크해도 고정비라고 따로 표시되지도 않고 정기결제내역에 반영되지도 않음"
- **경위**: v1 4445a9d 는 사용법 안내만 추가(R15). 직원은 **고정비 체크 → 고정비 라벨 미표시 + 정기결제내역 미반영** 버그를 새로 제기.
- **라우팅**: finance-agent — `src/app/(app)/bank/page.tsx`(고정비 체크 = 60420ce 자동이체 라벨) + `src/app/(app)/cards/page.tsx`(정기결제내역)
- **할 것**: 거래/카드를 고정비로 체크 시 (1) '고정비' 라벨이 실제 표시 (2) 정기결제내역에 반영. 체크 상태 저장→표시→정기결제내역 집계 경로 점검·수정.
- **하지 말 것**: BLOCKED(홈택스/현금영수증 codef 분기) 미접근.
- **완료 정의**: 고정비 체크 → 목록에 '고정비' 표시 + 정기결제내역에 해당 항목 반영.

### V6. 급여 배치 — "아니요"면 빈칸 (R4 추가 요구)
- **출처 카드**: 급여 배치 — 지난달 명세 복사 선택
- **직원 후속(verbatim)**: "물어보고 예라고 했을때는 그대로 복사 잘 되는데 아니요라고하면 빈칸으로 주세요"
- **경위**: v1 ac7fc81 로 복사 여부를 항상 물어봄(동작 OK). 추가 요구: **"아니요" 선택 시 지난달 복사 대신 빈칸(공란)으로 새 배치 생성**.
- **라우팅**: hr-agent — 급여 배치 생성 플로우(payroll)
- **할 것**: 복사 프롬프트 "아니요" 분기 → 지난달 값 미복사, 빈 배치 생성.
- **완료 정의**: 예=복사(현행 유지), 아니요=빈칸 생성.

---

## 🟡 P1 — 차단 해소: 직인 예시 이미지 직원이 재첨부함

### V7. 직인 — 직원 예시 이미지 확보 완료 ⚠️ v1 방향 정반대(사각 ❌ → 원형 ⭕)
- **출처 카드**: 직인 — 법인인감(원형) 형식
- **직원 후속(verbatim)**: "도장스타일은 이렇게 해달라는말이요" + 도장 예시 이미지 재첨부
- **★이미지 확보됨 (워크스페이스 로컬 — 코딩 클로드 바로 열람 가능)**:
  - `직인-예시-이미지\직인예시-1-toggle안.png` — **거부된 예시(이렇게 하지 말 것)**: 단일 원형 테두리 + 한글 가로쓰기 "모티브이/노베이션" + 작은 "印".
  - `직인-예시-이미지\직인예시-2-카드재첨부.png` — **★직원이 원하는 최종 형태**: **이중 원형 테두리**, 바깥 둘레를 따라 **회사명을 곡선(원호)으로 빙 둘러** 배열, **중앙 작은 원 안에 한자 「代表理事」(대표이사) 세로쓰기**. 실제 한국 법인 대표이사 인감도장 형식.
- **⚠️ v1 오구현 교정**: v1(447d91e)은 이미지가 없어 **"정사각 이중테두리·세로열"** 로 만들었음 → **방향이 정반대**. 직원이 원하는 건 **원형(circular) + 회사명 원호 배열 + 중앙 代表理事**. 사각형 아님. 기존 원호(arc) 헬퍼를 제거하지 말았어야 함 — 오히려 원호 텍스트가 핵심.
- **라우팅**: docs-agent — `src/lib/seal-generator.ts` corporate variant
- **할 것**: `직인예시-2-카드재첨부.png` 에 시각적으로 부합하게 corporate 직인 재구현 — ① 이중 **원형** 테두리 ② 회사명을 바깥 링을 따라 원호 배열(arc text 복원/재구현) ③ 중앙 소원 안 「代表理事」(직책 한자, 기존 sealPhrase 매핑 재활용) 세로쓰기. v1의 정사각 구현은 폐기.
- **하지 말 것**: 사각형 형태 금지. 이미지 없이 추측 금지(이제 이미지 있음 — 반드시 `직인예시-2` 기준). double/single/square 등 다른 variant·반환계약 회귀 금지.
- **완료 정의**: 생성된 corporate 직인이 `직인예시-2-카드재첨부.png` 와 시각적으로 부합(원형·회사명 원호·중앙 代表理事). 직원 재검수 통과.

---

## ⚪ 보류/대기 (직원 후속 없음, 우리측 사유)

### V8. 게시판 투표 복수/익명 — DB 마이그레이션 prod 적용 (R13·R14)
- **출처 카드**: 게시판 글쓰기 첨부 · **상태**: 코드(176f3e7)·스키마(04b7230) 푸시 완료, **마이그레이션 `20260519050000` prod 미적용** → 기능 미동작.
- **라우팅**: db-architect 선행 → ops-agent(board). **L 티어 + security-reviewer**.
- **할 것**: `supabase/migrations/20260519050000_board_poll_multi_anonymous.sql` 를 prod 적용(저트래픽 + advisors baseline + 적용 직후 게시판 투표 라이브 스모크). 묶음 board/page.tsx 는 이미 푸시됨(적용 전후 안전 설계). 적용 후 복수선택·익명/실명 실제 동작 확인.
- **완료 정의**: prod 적용 + 스모크 통과 → 투표 복수선택·익명/실명 실동작. 그 후 직원 안내.

### V9. R10 직원 프로젝트 탭 — 정책 미정 (변동 없음)
- 직원 후속 없음. 여전히 "직원에게 프로젝트 어디까지 노출" 정책 미정으로 보류. 정책 확정 시 growth-agent 진행.

---

## 처리 순서·도메인 묶음
1. **P0 먼저**: V1(급여이력 제거)·V2(딜 알림 미동작)·V3(거짓✅ 재검증+sticky).
2. P1: V4(내할일 관리자)·V5(고정비/정기결제 버그)·V6(급여배치 아니요)·V7(직인 이미지 확보 후).
3. 대기: V8(투표 DB 적용, L+security)·V9(정책).
- 도메인: hr(V1·V6) / growth+ops(V2) / ops(V3·V4) / finance(V5) / docs(V7) / db+ops(V8).
- **공통 지시**: 직원이 "거짓말"이라 했으므로, 처리 후 회신은 "반영함" 말로 끝내지 말고 **항목별 실제 동작 증거(실데이터/스크린/재현)** 를 붙일 것. 그래야 직원 재검수에서 신뢰 회복.

## 불확실 → 진행 중 판단(직원 재질의 대신 합리적 기본값, 회신 시 명시)
- V1: 직원·관리자 양쪽 급여이력 제거로 진행.
- V3: R1/R7/R8 중 무엇이 깨졌는지는 코딩 클로드가 prod 재현으로 특정.

## 완료 후
각 V항목 배포되면 commit hash + 화면위치 + **항목별 검증 증거** 를 이 창에 회신 → Notion 해당 카드 🔧 토글 직원 후속 아래에 결과 답신 + 직원 재검수 안내.
